# Oracle Incident Log - execution


- [2026-04-16 10:31:30] Oracle Incident - execution
  - 질문 맥락: rootcrs.sh -prepatch 와 rootcrs.sh -prepatch -nonrolling 차이와 nonrolling 이어도 실패하는 의미
  - 에러 메시지: (not provided)
  - 원인 가설: 사용자는 nonrolling 을 "모든 노드를 한 번에 prepatch 로 전환"으로 이해하고 있으나, Oracle 기준 nonrolling 은 전체 패치 세션 모드이며 12c 이후에도 첫 노드의 로컬 GI stack 은 살아 있어야 하고 원격 노드는 모두 내려가 있어야 세션이 시작된다. 따라서 nonrolling 으로도 실패하면 rolling 여부 문제가 아니라 prepatch 전제조건, 패치 레벨 불일치, ASM/cluster 상태, README 상 추가 절차 누락 같은 다른 원인이 남아 있을 가능성이 크다.
  - 권장 조치: 1) nonrolling 을 "동시 prepatch" 가 아니라 "클러스터를 혼합 패치 레벨 상태로 운용하지 않는 모드"로 해석한다. 2) 첫 노드 기준 로컬 CRS/GI up, 나머지 노드 CRS down 조건을 확인한다. 3) crsctl query crs activeversion -f, opatch lsinventory, cluvfy, README 요구사항, rootcrs/opatchauto 로그를 같이 본다. 4) 실제 실패 코드는 CRS-1159/1162/1166/1171 등으로 구분해 원인을 좁힌다.
  - 검증 항목: crsctl check crs; crsctl query crs activeversion -f; opatch lsinventory; GI/ASM 상태, 각 노드 up/down 상태, 패치 README 의 rolling/nonrolling 지원 여부, cfgtoollogs/opatchauto 및 rootcrs 로그 확인.
- [2026-04-16 10:40:00] Oracle Incident - execution
  - 질문 맥락: 로컬 노드 CRS/GI 가 내려가 있어도 rootcrs.sh -prepatch -nonrolling 이 오류 없이 수행되는 이유
  - 에러 메시지: (not provided)
  - 원인 가설: 앞선 "로컬 노드가 반드시 up" 조건은 OPatchAuto 가 첫 노드에서 nonrolling 세션을 시작할 때의 설명이며, 수동으로 실행하는 rootcrs.sh -prepatch -nonrolling 자체의 모든 경우에 그대로 대입하면 과한 일반화가 된다. rootcrs 는 rolling 모드에서는 rollingpatch 전환을 수행하지만, nonrolling 은 그 전환 없이 수동/논롤링 준비만 수행할 수 있어 로컬 CRS 가 이미 down 인 상태에서도 성공할 수 있다. shared GI home 에서는 rolling in-place patching 이 금지되어 nonrolling 또는 dstcrshome 으로 다시 실행하라는 Oracle 에러 도움말도 있다.
  - 권장 조치: rootcrs 단독 실행과 opatchauto orchestration 을 분리해서 해석한다. nonrolling 이 로컬 stack up 을 항상 강제한다고 단정하지 말고, 실제 환경에서는 1) shared GI home 인지 2) 수동 patch 절차인지 3) OPatchAuto 첫 노드 시작인지 4) patch README 가 manual stop/start 를 요구하는지로 판단한다.
  - 검증 항목: 사용한 명령이 opatchauto 인지 수동 rootcrs/opatch 인지 구분, GI home shared 여부, rootcrs 로그에서 rollingpatch 전환 시도 여부, patch README 의 nonrolling/manual 절차, crsctl query crs activeversion -f 와 관련 로그 확인.
- [2026-04-16 10:51:00] Oracle Incident - execution
  - 질문 맥락: rootcrs.sh -postpatch -nonrolling 자체의 의미와 왜 OPatchAuto 설명이 나오면 안 되는지 정리 요청
  - 에러 메시지: (not provided)
  - 원인 가설: 사용자는 수동 rootcrs postpatch 단계만 묻고 있다. Oracle 문서상 postpatch 는 GI home 을 다시 lock 하고 Clusterware stack 을 재기동하는 단계이며, -nonrolling 은 prepatch 에서 사용한 옵션과 동일하게 넘겨 세션 일관성을 맞추는 의미다. Oracle은 prepatch/postpatch 옵션 불일치(CLSRSC-00740)와 prepatch 미완료(CLSRSC-00739)를 별도로 막는다.
  - 권장 조치: rootcrs.sh -postpatch -nonrolling 은 "이전에 nonrolling 으로 연 prepatch 세션을 nonrolling 으로 마감한다"로 이해한다. 이 명령 자체를 rollingpatch 전환 조건과 섞지 말고, 1) prepatch 가 -nonrolling 으로 성공했는지 2) 모든 노드에 바이너리 패치가 끝났는지 3) 그 후 각 노드에서 동일 옵션으로 postpatch 를 수행하는지로 본다.
  - 검증 항목: CLSRSC-00739/00740 여부, rootcrs postpatch 로그, GI home 재잠금 여부, CRS/ASM 재기동 상태, 모든 노드 patch level 일치 여부 확인.
- [2026-04-16 11:02:00] Oracle Incident - execution
  - 질문 맥락: 논롤링의 의미 자체를 한 줄로 설명해 달라는 요청
  - 에러 메시지: (not provided)
  - 원인 가설: 사용자는 "노드 수"나 "동시 수행"이 아니라 패치 모드의 본질을 묻고 있다. Oracle에서 rolling 은 노드 간 patch level 이 일시적으로 달라도 cluster 가 이를 허용하는 모드이고, nonrolling 은 그 허용 상태로 운영하지 않는 모드다.
  - 권장 조치: nonrolling 을 "혼합 patch level 상태를 허용하지 않는 패치 방식"으로 이해한다. 즉 한 노드씩/여러 노드를 동시에의 의미가 아니라, 패치 중간에 cluster 전체를 정상 서비스 상태로 계속 유지하지 않고 최종적으로 모든 노드 patch level 을 맞춘 뒤 정상 상태로 복귀시키는 방식이다.
  - 검증 항목: rollingpatch 진입 여부, prepatch/postpatch 옵션 일치, 패치 완료 후 모든 노드 GI patch level 일치 여부 확인.
- [2026-04-16 11:12:00] Oracle Incident - execution
  - 질문 맥락: Grid 패치에서 모든 노드 CRS 를 내린 뒤 "모든 노드 패치를 한 번에" 어떻게 적용하는지 문의
  - 에러 메시지: (not provided)
  - 원인 가설: 사용자는 nonrolling 을 all-at-once 적용과 동일시하고 있으나, 실제로는 GI home 이 shared 인지 non-shared 인지에 따라 "한 번에"의 의미가 달라진다. shared GI home 은 물리적 Grid home 이 하나라서 바이너리 patch apply 를 한 번만 수행하면 되고, non-shared/private GI home 은 노드마다 로컬 Grid home 이 따로 있어 각 노드에서 opatch apply 를 각각 수행해야 한다.
  - 권장 조치: 1) shared GI home 이면 패치 바이너리는 공유 홈에 대해 한 번만 적용하고 동시에 여러 노드에서 같은 shared home 에 opatch apply 를 돌리지 않는다. 2) non-shared/private GI home 이면 각 노드 local Grid home 에 대해 opatch apply 를 실행해야 하며, "한 번에" 하고 싶으면 ssh/ansible/pdsh 같은 병렬 실행으로 각 노드에서 같은 patch apply 를 동시에 수행한다. 3) rootcrs.sh 는 pre/postpatch 및 lock/unlock/stack 처리용이지, 바이너리를 다른 노드로 fan-out 해서 apply 하는 명령이 아니다.
  - 검증 항목: shared/non-shared GI home 확인, 각 노드 ORACLE_HOME 경로 확인, opatch lsinventory 결과 일치, postpatch 후 모든 노드 GI patch level 일치 여부 확인.
- [2026-04-16 11:24:00] Oracle Incident - execution
  - 질문 맥락: 사용자가 정리한 OPatch 형식에 맞춰 OPatchAuto 명령 체계를 동일한 스타일로 정리 요청
  - 에러 메시지: (not provided)
  - 원인 가설: 사용자는 Oracle 공식 문서를 한국어 메모 형태로 재구성하려 한다. OPatchAuto 는 OPatch 와 달리 apply/resume/rollback/version 중심의 얇은 CLI 이며, GI/RAC 기준으로는 GI home 에서 root 로 실행해야 하고 System Patch 중심이라는 점이 핵심이다.
  - 권장 조치: OPatchAuto 를 1) 개요/제약 2) 글로벌 도움말 3) apply 4) resume 5) rollback 6) version 7) 주의사항 으로 나눠 정리한다. 특히 -rolling 이 일반 GI rolling 이 아니라 sharded database(sdb) rolling 옵션이라는 점, GI/RAC 논의에서는 -nonrolling 과 혼동하지 않도록 주석을 단다.
  - 검증 항목: Oracle 공식 OPatchAuto Syntax and Commands 문서의 apply/resume/rollback/version 구문과 옵션 설명, 12c 미만 one-off 비지원/System Patch 지원 범위, GI home/root 실행 요건 확인.
- [2026-04-16 11:29:00] Oracle Incident - execution
  - 질문 맥락: OPatchAuto 정리는 19c 기준으로 한정해 달라는 추가 요청
  - 에러 메시지: (not provided)
  - 원인 가설: 사용자는 여러 버전 혼합 설명이 아니라 19c 유지보수 기준에서 바로 써먹을 수 있는 정리를 원한다. 19c 기준에서는 RU/MRP 유지보수 문서와 OPatchAuto Syntax and Commands 문서를 함께 보는 것이 적절하다.
  - 권장 조치: 19c 기준 주석을 명시하고, apply/resume/rollback/version 문법은 Oracle 공식 OPatchAuto Syntax and Commands 기준으로 유지하되, RU/MRP 유지보수 관점에서 "MRP는 opatchauto 사용", "one-off는 보통 opatch"라는 구분을 덧붙인다.
  - 검증 항목: Oracle Database 19c Patch Maintenance 문서의 MRP/OPatchAuto 언급, GI home/root 실행 요건, apply/resume/rollback/version 구문 확인.
