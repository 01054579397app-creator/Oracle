[2026-08-12 10:30 KST]
- 주제: Echelon ECL_DECRYPT 결과가 암호문 그대로 반환되는 SQL 형태
- 질문 맥락: SQL*Plus 셸 스크립트에서 ECL_ENCRYPT 결과를 변수로 받아 ECL_DECRYPT에 전달했으나 암호문이 그대로 출력됨.
- 오류 메시지: ORA 오류 없음. `select 'Decrypt Text : '||ecl_decrypt('<cipher>') from dual`에서 입력 암호문이 그대로 반환됨.
- 원인: DB의 ECHELON.ECL_DECRYPT 함수는 입력을 그대로 반환하는 자리표시자이며, Echelon 프록시가 SQL을 인식할 때만 실제 복호화한다. 함수 결과 앞에 문자열 연결 연산자를 둔 형태는 프록시가 복호화 함수 호출로 인식하지 못했다. 변수는 32바이트로 공백 없이 정상임을 바이트 단위로 확인함.
- 권장 조치: ECL_DECRYPT를 SELECT 최상위 표현식으로 단독 호출한다. 같은 줄에 라벨이 필요하면 SQL*Plus `COLUMN ... NEW_VALUE ... NOPRINT`로 결과를 받은 뒤 `PROMPT`로 출력한다.
- 검증 항목: 단독 호출은 평문 `123`을 반환했고, 문자열 연결 호출은 암호문을 그대로 반환했다. `COLUMN DEC_TEXT NEW_VALUE DEC_TEXT NOPRINT` 후 `PROMPT Decrypt Text : &DEC_TEXT`는 `Decrypt Text : 123`을 출력했다.

[2026-08-12 11:05 KST] 추가 업데이트
- 주제: RAC 두 Echelon 노드의 암호키 교차 검증 스크립트
- 질문 맥락: DMHOME1과 DMHOME2에서 같은 원문을 각각 암호화한 뒤 각 노드에서 두 암호문을 모두 복호화하도록 스크립트를 구성함.
- 오류 메시지: 해당 없음.
- 원인 가설: 현재 출력 SELECT는 앞에서 저장한 두 암호문을 표시하지 않고 접속 노드에서 ECL_ENCRYPT를 다시 호출하므로, 표의 암호문이 교차 검증에 사용한 변수와 같은 값인지 화면에서 직접 확인하기 어렵다.
- 권장 조치: 출력 SELECT에서 `ecl_encrypt('$TEXT')` 대신 `'$ENCRYPT_TEXT_DMHOME1'`, `'$ENCRYPT_TEXT_DMHOME2'`를 각각 출력하고 두 값을 ECL_DECRYPT에 전달한다.
- 검증 항목: 두 암호문이 같고 양 노드에서 둘 다 원문으로 복호화되면 동일 데이터 암호키 사용을 강하게 입증한다. 암호문이 다르거나 상대 노드 암호문만 복호화되지 않으면 키 또는 함수 정책 차이를 조사한다.

[2026-08-12 11:30 KST] 추가 업데이트
- 주제: Echelon RAC 설치 정리 문서 검토
- 질문 맥락: 설치 파일, 포트 변경, 에이전트 설정, 관리도구 키 생성, DMHOME1/2 함수 테스트를 정리한 문서의 정확성을 검토함.
- 오류 메시지: 해당 없음.
- 원인 가설: 문서의 테스트 절에서 1521과 11521의 용도가 반대로 설명됐고, Echelon 프록시가 모든 조회 결과를 자동 복호화하는 것처럼 표현됨. 양 노드 동일 데이터 키 주입, RSA 키 쌍 보존, JDBC 버전 확인 및 교차 복호화 검증도 누락됨.
- 권장 조치: Echelon 기능 테스트는 1521, Oracle 직접 우회 진단은 11521로 명시한다. ECL 함수 또는 등록 정책을 프록시가 인식할 때 암복호화된다고 표현한다. RAC 노드별 관리 세션과 공통 데이터 키의 차이를 기록하고, 키 파일 삭제 금지 및 해시 검증을 추가한다.
- 검증 항목: 1521 접속에서 ECL_ENCRYPT/DECRYPT가 처리되고 11521 직접 접속에서는 DB 자리표시자 함수가 입력값을 반환하는지 확인한다. DMHOME1에서 생성한 암호문을 DMHOME2에서, DMHOME2 암호문을 DMHOME1에서 복호화한다.

[2026-08-12 13:15 KST] 추가 업데이트
- 주제: SQL Developer F5와 Ctrl+Enter의 Echelon 복호화 결과 차이
- 질문 맥락: 동일한 ECL_DECRYPT SELECT가 Ctrl+Enter에서는 복호화되지만 F5 Run Script에서는 암호문을 반환함.
- 오류 메시지: ORA 오류 없음.
- 원인 가설: Ctrl+Enter는 단일 Run Statement와 Query Result 경로를 사용하고, F5는 Script Runner와 Script Output 경로를 사용한다. 서버 로그에서 두 요청의 SQL 문자열이 Echelon에 도착한 것은 확인됐으므로, Echelon V4.5가 Script Runner의 실행 또는 결과 수신 방식을 정상 처리하지 못하는 호환성 문제로 추정한다.
- 권장 조치: SQL Developer에서 Echelon 암복호화 검증은 Ctrl+Enter를 사용하고, 배치 스크립트는 검증된 SQL*Plus 경로를 사용한다. F5 결과를 키 이상으로 판단하지 않는다.
- 검증 항목: Echelon 로그에 동일한 `select ecl_decrypt(object_name) from test`가 반복 수신됨. SQL 선행 공백 유무를 SQL*Plus에서 비교했으나 둘 다 정상 복호화되어 공백은 원인이 아님.

[2026-08-12 13:40 KST] 추가 업데이트
- 주제: Echelon RAC 설치 정리본 최종 검토
- 질문 맥락: 포트 원리, 에이전트 설정, 관리도구, RAC 키 배치 및 함수 테스트를 포함한 업데이트 문서를 검토함.
- 오류 메시지: 해당 없음.
- 원인 가설: 1521 접속 시 기존 값이 암호화되어 보인다는 표현, 11521 접속 시 사용자에게 잘 보인다는 표현, 두 ECL 함수를 모두 암호화 표식으로 설명한 부분이 부정확하다. Java 1.8 이상 표현과 설치 셸 명령도 실제 제품 환경에 비해 범위가 넓거나 불완전하다.
- 권장 조치: 1521은 ECL 함수 또는 정책을 처리하는 경로, 11521은 저장값을 그대로 보는 Oracle 직접 경로로 정의한다. Java는 검증된 1.8.0_501을 사용하고 `su - grid`, `cd /sw/echelon`을 명시한다. DB 객체 설치, 노드2 설정, 서비스 기동 검증 및 교차 복호화 절차를 추가한다.
- 검증 항목: 두 노드에서 1521/11521/11523/29995 리슨, RSA 주입 성공, ECHELON 객체 상태, DMHOME1/2 교차 복호화 성공 여부를 확인한다.
