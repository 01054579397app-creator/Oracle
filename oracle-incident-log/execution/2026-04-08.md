# Oracle Incident Log - execution


- [2026-04-08 09:14:07] Oracle Incident - execution
  - 질문 맥락: OGG CDC는 동작하지만 초기 적재가 누락된 상태에서 expdp/impdp와 Replicat 재기동을 반복 검증했고, 최종적으로 source 테이블에 primary key(id)를 추가한 뒤 source ggadmin.ogg와 target ggadmin.ogg_source의 건수가 일치함. 초기 적재와 CDC를 분리해 이해해야 한다는 점을 재정리함.
  - 에러 메시지: ORA 에러는 명시되지 않았고, 증상은 target count 불일치 및 replace 수행 시 중복 1건 발생
  - 원인 가설: 초기 적재 없이 CDC만 따라가면서 기존 행이 비어 있었고, 행 식별 키 부재로 replace/재적재 이후 중복 또는 오적용이 발생했을 가능성이 큼
  - 권장 조치: 초기 적재와 CDC를 분리해 runbook화하고, stop rep -> expdp/scp/impdp -> start rep 흐름과 source PK 선행 여부를 점검한다. Source/Target OGG 파라미터와 supplemental logging, force logging, enable_goldengate_replication도 함께 검토한다.
  - 검증 항목: source ggadmin.ogg와 target ggadmin.ogg_source count를 반복 조회해 최종 1177/1177 일치 확인. 향후에는 PK 존재, initial load 완료 시점, Replicat 재기동 직후 중복 여부를 함께 검증한다.
- [2026-04-08 09:17:13] Oracle Incident - execution
  - 질문 맥락: OGG 스크립트 기반 재정리 문서를 integrated_main_v3 content-graph JSON으로 구조화하고, 설치/초기설정/Extract/Replicat/검증 흐름과 옵션 목록을 보존해 달라는 요청
  - 에러 메시지: (not provided)
  - 원인 가설: 운영 중 테이블 추가 절차는 원문에 없고, Replicat 구성의 dblogin useridalias src는 trg intended일 가능성이 있어 문서 확인이 필요함
  - 권장 조치: 환경 준비, DB 파라미터, OGG 설치/패치, Manager, Integrated Extract/Replicat, 검증 명령, 옵션 리스트, 내일 진행 항목을 분리 구조화한다. 원인 분석과 테스트 관찰은 별도 필드로 나눈다.
  - 검증 항목: core_questions, sections, causal_chains, observations, reference_options, next_tasks가 원문과 대응되는지 확인한다.
- [2026-04-08 09:17:27] Oracle Incident - execution
  - 질문 맥락: OGG integrated capture에서 ADD EXTRACT SCN, REGISTER EXTRACT SCN, instantiation CSN의 차이와 PREPARECSN/ENABLE_INSTANTIATION_FILTERING 조건을 실험 결과 기준으로 구조화 정리함
  - 에러 메시지: OGG-02085 dictionary locate failure at non-dictionary-begin SCN, 복제 결과 불일치 관찰
  - 원인 가설: REGISTER EXTRACT SCN은 dictionary begin SCN에 정확히 맞아야 하며, 초기 적재 시에는 per-table instantiation CSN과 ENABLE_INSTANTIATION_FILTERING 조합이 핵심이고, 빈 타깃에서 과거 변경 재생은 별도 패턴으로 봐야 함
  - 권장 조치: 원인 분석과 테스트 관찰을 분리하고, dictionary build/preparecsn/initial load 조합과 filtering 사용 조건을 문서화함
  - 검증 항목: DICTIONARY_BEGIN exact SCN 등록 성공 여부, PREPARECSN 적용 후 source/target count 일치 여부, 빈 타깃에서 filtering 제거 시 재생 패턴 검증
