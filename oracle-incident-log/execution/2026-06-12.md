# Oracle Incident Log - execution

- [2026-06-12 15:13:25] Oracle Incident - execution
  - 질문 맥락: RMAN으로 백업받을 때 백업 파일의 예상 최대 용량을 사전에 알 수 있는지 확인.
  - 에러 메시지: 해당 없음.
  - 원인 가설: RMAN backupset은 unused block compression, 압축 옵션, 증분 백업 여부, archivelog 포함 여부, backup piece 분할 방식에 따라 실제 출력 크기가 달라져 백업 시작 전 정확한 파일 크기를 계산하기 어렵다.
  - 권장 조치: 정확한 예측값 대신 datafile/archivelog 크기로 최악 상한을 잡고, 과거 `V$RMAN_BACKUP_JOB_DETAILS`의 `INPUT_BYTES`, `OUTPUT_BYTES`, `COMPRESSION_RATIO`로 실제 예상치를 보정한다. 개별 backup piece의 최대 크기는 `CONFIGURE CHANNEL ... MAXPIECESIZE` 또는 `ALLOCATE CHANNEL ... MAXPIECESIZE`로 제한한다.
  - 검증 항목: `DBA_DATA_FILES`, `DBA_TEMP_FILES`, `V$ARCHIVED_LOG`로 입력 크기를 확인하고, `V$RMAN_BACKUP_JOB_DETAILS`, `V$BACKUP_PIECE`에서 최근 백업 출력 크기와 piece별 최대 크기를 비교한다.
