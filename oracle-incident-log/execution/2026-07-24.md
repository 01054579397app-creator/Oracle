# Oracle Incident Log - execution

## 추가 업데이트 - RMAN 19c 연습장 완료 문장 해설과 잘못된 명령 조합 차단

- [2026-07-24 08:46:50] RMAN 19c 명령어 연습장 후속 수정
  - 질문 맥락: 실행용 또는 실행 차단용 도구가 아니라 모든 RMAN 명령을 연습하는 콘텐츠이므로 관련 안내문을 제거하고, 완성 문장 뒤 Enter를 누르면 해당 명령의 뜻을 모두 보여 달라는 요청. 사용자 화면에서 `BACKUP LIST`가 다음 입력 가능 문법처럼 표시되는 오류도 확인함.
  - 에러 메시지: `BACKUP LIST`는 Oracle RMAN 19c의 유효한 BACKUP 문법이 아니지만 기존 자동완성 엔진이 계속 입력 가능한 경로로 판정함.
  - 원인 가설: 모든 명령 NFA를 동시에 검사하면서 SQL 문법의 넓은 경로가 `BACKUP`으로 시작한 입력까지 받아들였고, 완료 처리도 명령 해설 대신 실행 차단 안내만 출력함.
  - 권장 조치: 첫 번째 명령어가 정해지면 같은 최상위 명령의 NFA만 검사하도록 공통 파서를 제한함. 실행 차단과 미연결 안내를 제거하고 화면을 `RMAN 명령어 연습장`으로 변경함. 완성 문장 Enter 시 문법 경로, 명령 뜻, 개요, 사용 범위, 주의점, 19c 기준을 출력함. 잘못된 조합은 오른쪽 설명과 Enter 결과에서 명시적으로 오류 표시함.
  - 검증 항목: `BACKUP` 후보에 `LIST`가 없고 `BACKUP LIST`는 `matched=false`임. `BACKUP DATABASE;` 해설에 뜻, 개요, 사용 범위, 주의점이 모두 존재함. 전체 54개 명령 예제 회귀 검사와 Node 검증을 통과하고 Edge 1440x900 화면에서 기존 실행 차단 및 미연결 문구가 제거된 것을 확인함. HTML SHA256 `8E24ED77595751937559A2BC3CDE165FE39C4309AFC8F7390E511439A0E3CB0B`.

## 추가 업데이트 - BACKUP DB_FILE_NAME_CONVERT 의미

- [2026-07-24 08:49:23] `DB_FILE_NAME_CONVERT` 하위 절 설명
  - 질문 맥락: RMAN 연습장에서 `BACKUP` 다음 후보로 표시되는 `DB_FILE_NAME_CONVERT`가 무엇인지 문의함.
  - 에러 메시지: 없음. 정확한 키워드는 끝에 `T`가 포함된 `DB_FILE_NAME_CONVERT`임.
  - 원인 가설: 이 키워드는 일반 백업 피스 이름을 지정하는 `FORMAT`과 달리, 기존 데이터파일 이름의 문자열을 치환하여 새 출력 파일명을 만드는 `fileNameConversionSpec`이므로 용도가 직관적이지 않음.
  - 권장 조치: 주로 `BACKUP AS COPY` 이미지 복사본에서 `(원본문자열, 대체문자열)` 쌍을 짝수 개 지정함. RMAN은 원본 파일명에서 첫 번째로 일치한 원본문자열을 대응 대체문자열로 바꿈. 예: `BACKUP AS COPY DB_FILE_NAME_CONVERT=('dbs','newdbs') TABLESPACE users;`는 `/disk1/dbs/users.dbf`를 `/disk1/newdbs/users.dbf` 형태로 생성함.
  - 검증 항목: Oracle Database 19c Backup and Recovery Reference 4.8 `fileNameConversionSpec`에서 BACKUP, CONVERT, DUPLICATE의 출력 파일명 생성에 사용함을 확인함. 여러 쌍은 첫 일치 항목이 적용되며, ASM 또는 일반 파일시스템의 OMF 원본은 이 절로 새 OMF 이름을 만들 수 없으므로 `FORMAT` 등으로 위치를 지정하고 데이터베이스가 이름을 생성하게 해야 함.

## 추가 업데이트 - RMAN 19c 전체 BNF 가지 감사와 경로별 설명

- [2026-07-24 09:17:53] 54개 명령의 전체 파생 문법 전수 검사
  - 질문 맥락: 일부 예제만 맞추는 방식이 아니라, 하나의 명령 뒤에서 파생되는 모든 공식 19c 문법 가지가 실제로 선택·종료되고 현재 경로에 맞는 설명을 표시하는지 명령별로 전부 확인해 달라는 요청.
  - 에러 메시지: 기존 엔진은 NFA가 18,000상태를 넘으면 남은 문법을 epsilon으로 대체하여 `RUN` 문법 뒤쪽을 조용히 누락함. 공식 `Syntax Element / Description` 표도 빌드 과정에서 버려져 `DB_FILE_NAME_CONVERT` 같은 후보의 설명이 일반 문구로 표시됨. Oracle 그림의 텍스트 대체 문법에는 `RESTORE`, `DUPLICATE`, `RMAN`, `SWITCH`, `UNREGISTER`, 문자열 변환 쌍의 괄호가 깨진 항목도 존재함.
  - 원인 가설: 예제 NFA와 공식 NFA를 함께 허용하고, `...`를 0회 이상으로 해석하며, 점(`schema.table`)을 값 자리표시자로 분류한 파서 문제가 공식 BNF의 누락·과잉 허용을 숨김. 구분자 불일치와 루트에서 연결되지 않은 보조 문법을 검사하는 단계도 없었음.
  - 권장 조치: 예제 NFA를 제거하고 공식 BNF만 사용함. 상태 상한을 제거하고 `...`를 1회 이상으로 교정했으며 점·콜론·예약어·문자열 변환 쌍을 정확히 토큰화함. 공식 페이지의 874개 고유 의미 행을 내장하고 후보의 원래 문법과 현재 입력 경로를 함께 사용해 오른쪽 설명과 완료 명령 해설을 변경함. 깨진 공식 텍스트 도표만 해당 페이지의 공식 선택지와 예제에 맞춰 정규화하고, 모든 보조 문법이 명령 루트에서 도달하는지 검사함.
  - 검증 항목: 54개 명령, 132개 BNF, 77,048개 NFA 상태, 20,615개 전이를 전수 검사함. 모든 명령에서 미도달 상태·종료 불가 상태·종료 불가 가지·출처 없는 가지·설명 없는 가지가 0개임. `BACKUP DB_FILE_NAME_CONVERT`는 `=`, `(` 또는 문자열 쌍 형식을 제시하고, `= (` 뒤에서 두 문자열과 닫는 괄호를 완료한 후에도 백업 대상을 요구하며 `DATABASE` 이후에만 `;`를 제시함. 54개 명령의 학습 예제가 공식 NFA에서 모두 완성되고 Node 회귀 검사를 통과함. 최종 HTML SHA256 `47F73A208B274C5B0DCBFCF23193D34A6A7DEA1030227BFE5F66280A3F4E8197`. Chrome 자동 화면 검증은 로컬 `file://` URL 정책으로 수행하지 못했으므로 실제 화면 배치는 사용자가 열린 탭을 새로고침하여 확인해야 함.

## 추가 업데이트 - 자동완성 후보 위쪽 방향키 이동 수정

- [2026-07-24 09:26:31] `Tab` 후보 목록의 `↑` 이동 오류
  - 질문 맥락: `Tab`으로 자동완성 후보를 연 뒤 `↓` 이동은 되지만 `↑`로 이전 후보에 돌아가지 않는 현상을 수정해 달라는 요청.
  - 에러 메시지: 오류 메시지는 없으며, 선택 강조가 위쪽 방향키 입력 후 이전 항목으로 유지되지 않는 UI 동작 오류임.
  - 원인 가설: 방향키를 누를 때마다 `openCompletion()`이 후보 DOM 전체를 다시 만들었고, 재생성된 항목의 마우스 진입 이벤트가 키보드로 계산한 선택 인덱스를 다시 덮어쓸 수 있었음.
  - 권장 조치: 공통 선택 함수는 현재 DOM의 활성 항목과 설명만 갱신하게 하고, `moveCandidate()`에서는 후보 목록을 재생성하지 않도록 변경함. 후보 미리보기 이벤트도 DOM 이동으로 발생할 수 있는 `mouseenter` 대신 실제 포인터 이동인 `mousemove`에서만 처리함.
  - 검증 항목: `Tab → ↓ → ↓ → ↑`에 해당하는 인덱스가 `0 → 1 → 2 → 1`인지 내장 검사와 Node 회귀 검사로 확인함. 방향키 함수가 `openCompletion()`을 호출하지 않는 조건도 검사하며, 54개 명령·874개 공식 의미 행·77,048개 문법 상태·20,615개 전이 전체 검증을 통과함. HTML SHA256 `A4F110646207AE37CBE09EA45C42618059A7F3FD9AEF30176548DBA9C863750B`.

## 추가 업데이트 - RMAN 19c 문법 완성과 실행 전제조건 분리 검증

- [2026-07-24 09:45:56] 54개 명령과 전체 종료 가지의 실행 맥락 전수 검사
  - 질문 맥락: `LIST SCRIPT NAMES;`는 문법상 완성되지만 대상 데이터베이스와 복구 카탈로그에 모두 연결된 경우에만 사용할 수 있으므로, 모든 RMAN 명령의 각 완료 상태를 문법 완성 여부와 실제 실행 전제조건으로 나누어 하나씩 검증해 달라는 요청.
  - 에러 메시지: ORA 오류는 없음. 기존 연습장은 공식 문법만으로 종료 가능 여부를 판정하고 명령 페이지의 `Purpose`, `Prerequisites`, `Usage Notes`를 버려서, 문법상 유효한 문장을 조건 없이 실행 가능한 것처럼 오해할 수 있는 콘텐츠 검증 결함이 있었음.
  - 원인 가설: 문법 NFA의 완료 상태와 실행 환경 조건을 하나의 상태로 취급했고, 검증기도 예제가 문법에 매칭되는지만 검사하여 대상 DB·복구 카탈로그·MOUNT/OPEN·NOMOUNT·RUN 블록·RMAN 프롬프트·운영체제 명령행 같은 전제조건을 확인하지 않았음.
  - 권장 조치: Oracle Database 19c Backup and Recovery Reference의 각 명령 페이지에서 `Purpose`, `Prerequisites`, `Usage Notes`, 공식 예제를 구조화하여 내장함. 화면과 Enter 결과를 `19c 문법상 완성`과 `공식 실행 전제조건`으로 분리하고, 현재 입력 경로의 공식 `Syntax Element / Description` 조건을 함께 표시함. `LIST SCRIPT NAMES;`는 대상 DB와 복구 카탈로그가 모두 필요하고, `LIST ALL SCRIPT NAMES;` 및 `LIST GLOBAL SCRIPT NAMES;`는 복구 카탈로그만 필요하며 대상 DB 연결은 불필요하다고 구분함. 잘못 완료 처리되던 `RESET DATABASE;`, `LIST INCARNATION OF DATABASE;`를 미완성으로 교정하고 `RUN`, `CREATE SCRIPT`, `REPLACE SCRIPT`의 중괄호·내부 세미콜론 종료 규칙도 공식 19c 페이지에 맞춤.
  - 검증 항목: 54개 명령, 170,720개 NFA 상태, 45,693개 문법 전이, 2,422개 종료 직전 가지, 384개 공식 조건 행을 검사함. 미도달·종료 불가·설명 없음·19c 출처 없음·문법에 연결되지 않은 조건 행은 모두 0개임. 54개 명령 모두 공식 실행 맥락을 보유하고 전체 학습 예제가 `matched=true`, `complete=true`이며 Enter 해설에 공식 실행 맥락이 포함되는지 Node 회귀 검사로 확인함. Python 구문 검사도 통과함. 최종 자체 포함 HTML 크기는 1,006,763바이트, SHA256은 `1200212E93232F892199C6127949C958A20A49E559C13FB088FA621E701E4C53`임.

## 추가 업데이트 - 자동완성 활성 후보가 상단에서 가려지는 문제 수정

- [2026-07-24 13:52:11] 방향키 선택 행의 스크롤 추적 보정
  - 질문 맥락: `Tab` 후보 목록에서 `↑` 키로 선택 자체는 이동하지만, 선택된 행이 후보창 위쪽으로 올라가면 화면에 보이지 않아 현재 선택 내용을 알 수 없는 현상을 수정해 달라는 요청.
  - 에러 메시지: 오류 메시지는 없음. 키보드 선택 인덱스와 강조 상태는 정상이나 활성 행의 가시 영역 추적이 실패한 UI 오류임.
  - 원인 가설: 후보창 전체가 스크롤 컨테이너이고 헤더가 `position: sticky`로 그 위를 덮는 구조였음. `scrollIntoView({block:"nearest"})`는 활성 행이 스크롤 영역 안에 있다고 판정하지만, 위로 이동할 때 행이 고정 헤더 아래에 가려질 수 있었음.
  - 권장 조치: 후보창을 세로 Flex 컨테이너로 변경하고 헤더는 고정된 비스크롤 영역, `#candidateList`만 독립 스크롤 영역으로 분리함. 기존 공통 `selectCandidate()`의 `scrollIntoView({block:"nearest"})`가 활성 행을 후보 목록 안에서 직접 추적하게 하여 `↑`와 `↓` 모두 현재 선택 행이 보이도록 함.
  - 검증 항목: Python 구문 검사, HTML 재생성, Node 전체 회귀 검사를 통과함. 검증 스크립트에 후보창 Flex 구조, 후보 목록 독립 `overflow:auto`, 활성 행 `scrollIntoView()` 호출이 함께 유지되는지 검사하는 조건을 추가함. 생성 HTML은 1,006,851바이트이고 SHA256은 `CB334B85ED8DC70B6EEACE24D46443216F7E903DEE4685E1C45A11EDE5EB0ED0`임. Chrome 자동 화면 검증은 로컬 `file://` URL 보안 정책으로 수행하지 않았으므로 열린 탭을 새로고침한 뒤 실제 키보드 동작을 확인해야 함.

## 추가 업데이트 - BACKUP AS BACKUPSET FORMAT 순서 증명과 한글 가독성 개선

- [2026-07-24 14:02:04] 전체 자동완성 전이의 공식 BNF 근거 감사
  - 질문 맥락: `BACKUP AS BACKUPSET` 다음 후보에 `FORMAT`이 나오는 것이 맞는지 확인하고, 특정 예제만이 아니라 모든 자동완성 순서를 하나씩 공식 문법으로 검증·증명해 달라는 요청. 경로별 공식 실행조건과 기타 상세 내용이 영문 위주이고 긴 BNF가 모두 펼쳐져 가독성이 낮다는 지적도 함께 반영함.
  - 에러 메시지: ORA 오류는 없음. `FORMAT` 후보 자체는 유효하지만 기존 화면에는 왜 그 위치에서 가능한지 보여 주는 문법 출처가 없었고, 공식 조건 원문을 영어로 바로 노출해 학습 흐름을 방해했음.
  - 원인 가설: Oracle 19c `BACKUP` 문법에서 `AS BACKUPSET`은 `backupTypeSpec`이고 `FORMAT formatSpec`은 대상보다 먼저 반복 지정할 수 있는 `backupOperand`임. 따라서 `BACKUP AS BACKUPSET FORMAT '/backup/%U' DATABASE;`는 유효하지만 `FORMAT` 값까지만 입력한 상태는 `backupSpec` 대상이 없어 종료할 수 없음. 기존 검증은 전이의 도달·종료 여부를 검사했으나 각 전이가 공식 BNF의 어느 토큰에서 생성됐는지 별도 수치로 증명하지 않았음.
  - 권장 조치: 모든 NFA 전이에 대해 소유 문법, Oracle 19c 출처, 원본 BNF 토큰 일치를 독립 검사하고 후보 선택 시 `왜 이 후보가 가능한가`와 `명령 → 하위 문법 → 후보` 경로를 표시함. `BACKUP AS BACKUPSET → FORMAT → formatSpec → backupSpec` 순서를 한글로 명시하고 `FORMAT` 뒤에는 `DATABASE`, `DATAFILE`, `TABLESPACE` 같은 대상이 필요하며 조기 `;`는 허용하지 않음. 실행조건은 한글 요약을 먼저 표시하고 정확성 확인용 영문 원문은 접었으며, 전체 BNF·공식 원문도 기본 접힘으로 변경함.
  - 검증 항목: 54개 명령의 170,720개 상태와 45,693개 자동완성 전이를 감사하여 공식 19c BNF 토큰으로 증명된 전이가 `45,693/45,693`임을 확인함. 2,422개 종료 가지와 384개 조건 행도 유지되며, 조건 행 384개 모두 한글 요약 생성 검사를 통과함. `BACKUP AS BACKUPSET` 직후에는 `FORMAT`과 백업 대상이 존재하고 `;`는 없으며, `FORMAT '/backup/%U'` 뒤에도 대상이 필요하고 `DATABASE` 뒤에만 완료되는 회귀검사를 추가함. Python 구문 검사와 Node 전체 검증을 통과했으며 HTML은 1,018,412바이트, SHA256은 `0A76368A708EDF4666C7386B17CAD14259F6AE15D9943E2995CDAA15442448BA`임.

## 추가 업데이트 - 실행조건 과다 표시와 AS BACKUPSET 조건 오분류 교정

- [2026-07-24 15:32:42] 현재 문법 경로 안내를 간결한 한글 카드로 재구성
  - 질문 맥락: `명령 공통 조건`이 수십 번 반복되고 긴 공식 영문이 행마다 노출되어 읽기 어려우며, 현재 선택한 절에 직접 해당하는 내용을 한국어로 간단히 정리해 달라는 요청.
  - 에러 메시지: ORA 오류는 없음. `AS BACKUPSET` 설명 안의 `COMPATIBLE 10.2`가 사용 전제조건처럼 표시됐으나, 공식 문서에서 이 값은 미사용 블록 압축 자동 적용 조건 중 하나이지 `AS BACKUPSET` 사용 조건이 아님.
  - 원인 가설: 공식 `Prerequisites`를 문장 단위로 분리하고 `must`, `cannot`, `COMPATIBLE` 같은 단어가 있으면 전체 문장을 실행조건으로 바꾸는 범용 요약기가 긴 기능 설명까지 조건으로 오인함. 각 행마다 공식 영문 원문 카드를 중첩해 같은 제목과 내용도 반복됨.
  - 권장 조치: 범용 조건 번역기와 반복 영문 카드를 삭제함. 현재 경로 화면은 `뜻`, 선택 후보의 `문법 근거`, 직접 적용되는 `필수조건·제한`, `다음 단계`, 공식 링크만 표시함. `AS BACKUPSET`에는 백업 세트의 성격과 테이프·레벨 1 제한만 남기고 `COMPATIBLE 10.2`를 제거함. `FORMAT`에는 파일명 패턴, 고유 이름, `KEEP`의 `%U`, FRA/OMF 제한만 두 줄로 정리함. Enter 완료 결과도 한글 뜻·개요·사용 범위·직접 제한·입력 절의 뜻·공식 링크만 출력함.
  - 검증 항목: 실제 렌더링 문자열로 `BACKUP AS BACKUPSET` 카드와 `FORMAT` 후보 카드, `BACKUP AS BACKUPSET FORMAT '/backup/%U' DATABASE;` Enter 결과를 검사함. 반복 제목, `명령 공통 조건`, 잘못된 `COMPATIBLE` 조건, `실행 판정`이 없고 `FORMAT` 자동 값으로 `'/backup/%U'`가 들어가는지 확인함. 54개 명령, 170,720개 상태, 45,693개 전이(`45,693/45,693` 공식 BNF 증명), 2,422개 종료 가지, 384개 공식 조건 행 전체 검증을 통과함. HTML은 1,014,425바이트, SHA256은 `19B2985C4C1C9B64559CDEFE3E6A111CB0CE174CD89FF8F064ED98AD0BE754C5`임.
