# Oracle Incident Log - execution


- [2026-04-13 17:21:14] Oracle Incident - execution
  - 질문 맥락: 현재 사용 중인 undotbs를 오프라인 후 드롭하는 방법
  - 에러 메시지: (not provided)
  - 원인 가설: 사용 중인 UNDO tablespace는 직접 오프라인/드롭이 불가하므로 새 UNDO로 전환 후 기존 UNDO가 PENDING OFFLINE에서 OFFLINE으로 바뀐 뒤 DROP 해야 함.
  - 권장 조치: CREATE UNDO TABLESPACE 새이름 ...; ALTER SYSTEM SET UNDO_TABLESPACE=새이름; 기존 UNDO 상태를 DBA_TABLESPACES에서 확인하고 OFFLINE이 되면 DROP TABLESPACE 기존이름 INCLUDING CONTENTS AND DATAFILES; 실행.
  - 검증 항목: SHOW PARAMETER undo_tablespace; SELECT tablespace_name,status,contents FROM dba_tablespaces WHERE contents='UNDO'; SELECT file_name FROM dba_data_files WHERE tablespace_name=UPPER('기존UNDO명');
- [2026-04-13 17:22:12] Oracle Incident - execution
  - 질문 맥락: UNDO 테이블스페이스에서 특정 데이터파일만 제거하는 방법
  - 에러 메시지: (not provided)
  - 원인 가설: 사용자는 UNDO 전체가 아니라 특정 datafile 1개만 삭제하고 싶어 함. 그러나 DROP DATAFILE은 파일이 empty, first/only 아님, offline 아님 조건이 필요하며 UNDO에서는 해당 조건 충족이 어려움.
  - 권장 조치: 일반 구문은 ALTER TABLESPACE <tablespace_name> DROP DATAFILE ''<file>''; 이지만 UNDO datafile은 보통 empty 만들기 어렵다. 활성 UNDO라면 새 UNDO tablespace 생성 후 ALTER SYSTEM SET UNDO_TABLESPACE=새UNDO 로 전환하고, 기존 UNDO tablespace 전체를 DROP TABLESPACE ... INCLUDING CONTENTS AND DATAFILES 하는 방식이 현실적이다.
  - 검증 항목: SELECT file_id,file_name,tablespace_name,status FROM dba_data_files WHERE tablespace_name=UPPER(''UNDOTBS1''); SELECT COUNT(*) FROM dba_extents WHERE file_id=<target_file_id>; 파일이 0 extents 이고 first/only file 가 아니며 online 인지 확인.
- [2026-04-13 17:23:45] Oracle Incident - execution
  - 질문 맥락: UNDO 특정 데이터파일이 non-empty일 때 drop 조건을 만들 수 있는지
  - 에러 메시지: (not provided)
  - 원인 가설: 사용자는 empty 조건을 만드는 절차를 원하지만, UNDO는 schema object를 이동/삭제해서 특정 datafile만 비우는 지원 절차가 없고 file-level evacuation이 사실상 불가함.
  - 권장 조치: Oracle 문서 기준 DROP DATAFILE은 empty file 에만 가능하며, empty 로 만들 수 없으면 tablespace 자체를 drop 해야 한다. UNDO는 add/rename/online-offline 정도만 관리 대상으로 안내되므로 특정 non-empty datafile만 제거하려면 새 UNDO tablespace 생성, ALTER SYSTEM SET UNDO_TABLESPACE 전환, 기존 UNDO tablespace 전체 DROP/재생성이 권장 절차다. 공간만 줄일 목적이면 ALTER DATABASE DATAFILE ... RESIZE 가능 여부를 별도 확인.
  - 검증 항목: SELECT tablespace_name,status FROM dba_tablespaces WHERE contents=''UNDO''; SELECT file_id,file_name,bytes/1024/1024 MB FROM dba_data_files WHERE tablespace_name=UPPER(''UNDOTBS1''); 필요 시 DBA_EXTENTS로 대상 file_id 사용 extent 존재 여부와 resize 가능 구간 확인.
