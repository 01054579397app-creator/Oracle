# Oracle Incident Log - execution


- [2026-07-14 08:46:11] Oracle Incident - execution
  - 질문 맥락: SQL*Plus heredoc 이후 Enter value for 1 프롬프트가 표시되는 이유
  - 에러 메시지: Enter value for 1:
  - 원인 가설: 호출된 SQL 파일 또는 그 하위 스크립트가 정의되지 않은 &1 치환변수를 참조한다. heredoc 내부 @script 뒤의 셸 리다이렉션 표기도 SQL*Plus 문맥에 잘못 들어가 있다.
  - 권장 조치: 호출 SQL 파일에서 &1 또는 &&1을 검색한다. @script 줄 뒤의 >> "$LOGFILE" 2>&1을 제거하고 sqlplus 명령 자체의 리다이렉션만 유지한다. 인수가 필요한 스크립트이면 명시적으로 값을 넘기고, 불필요한 치환이면 제거한다.
  - 검증 항목: grep -nE "&&?1([^0-9]|$)" 대상 SQL 및 하위 호출 파일; 수정 후 로그에 Enter value for가 없고 SQL*Plus 및 impdp 오류가 없는지 확인
- [2026-07-14 11:15:32] Oracle Incident - execution
  - 질문 맥락: Database Link를 이용한 CTAS가 가능한지
  - 에러 메시지: (not provided)
  - 원인 가설: DB Link 객체 자체는 CTAS 대상이 아니지만, 원격 테이블 SELECT 결과를 이용한 로컬 CREATE TABLE AS SELECT는 가능하다.
  - 권장 조치: CREATE TABLE local_table AS SELECT columns FROM remote_table@db_link 구문을 사용한다. 원격 데이터의 일회성 복사이며 PK, 인덱스, FK, 트리거, 권한은 별도로 생성한다.
  - 검증 항목: 원격과 로컬 건수 비교 및 로컬 테이블의 제약조건과 인덱스 확인
