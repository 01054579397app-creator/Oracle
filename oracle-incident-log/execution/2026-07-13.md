# Oracle Incident Log - execution


- [2026-07-13 14:11:59] Oracle Incident - execution
  - 질문 맥락: AS-IS의 UTL_HTTP HTTPS 서비스를 TO-BE에서 그대로 유지하려면 일반 DB 권한 외에 무엇을 이관해야 하는지 구체적인 이관 대상 요청
  - 에러 메시지: 오류 없음. 기존 답변이 Oracle 전체 권한 종류로 범위를 잘못 확장하여 UTL_HTTP 서비스 이관 항목으로 재정리
  - 원인 가설: 서비스 유지에는 ACL 외에도 PL/SQL 호출 코드와 Wallet 경로, OS Wallet 파일, CA/클라이언트 인증서와 개인키, 인증정보, Scheduler 실행정보, DNS/방화벽/라우팅/프록시, 외부 서버의 TO-BE 출발지 IP 허용, Oracle OS 파일 권한을 함께 이전해야 한다.
  - 권장 조치: AS-IS에서 서비스 계정/PDB/URL/포트/호출 패키지/Wallet 경로/Wallet 파일/인증서/작업 스케줄/네트워크 허용 정책을 인벤토리화한다. TO-BE에는 DB 객체와 ACE를 반영하고 Wallet 디렉터리를 안전하게 복제하거나 재생성한 뒤 소유권과 권한을 설정한다. DNS, 방화벽, 프록시, 외부 서버 IP allowlist를 TO-BE 기준으로 변경한다.
  - 검증 항목: TO-BE 실제 서비스 계정과 PDB에서 동일 URL에 UTL_HTTP 호출하여 HTTP 응답을 확인하고, 실제 SOAP 요청과 Scheduler Job까지 실행한다. AS-IS/TO-BE ACL 및 orapki wallet display 결과와 코드 내 URL/wallet_path를 비교한다.
- [2026-07-13 14:15:22] Oracle Incident - execution
  - 질문 맥락: Oracle Data Pump INCLUDE 목록에 Network ACL과 ACE를 이관할 수 있는 Object Path가 있는지 문의
  - 에러 메시지: 오류 없음. Data Pump 메타데이터 Object Path 확인 문의
  - 원인 가설: Full Data Pump 모드의 DATABASE_EXPORT_OBJECTS에는 NETWORK_ACL Object Path가 있으며 Host ACL, Wallet ACL 및 ACE가 NACL export/import 내부 객체로 함께 처리된다. ACE와 ACL은 별도 INCLUDE 이름이 아니고 NETWORK_ACL 하나로 묶인다. Schema/Table 모드에는 일반적으로 사용할 수 없으므로 실제 DB 뷰 조회가 기준이다.
  - 권장 조치: AS-IS PDB에서 DATABASE_EXPORT_OBJECTS와 SCHEMA_EXPORT_OBJECTS를 조회한다. NETWORK_ACL이 확인되면 expdp FULL=Y INCLUDE=NETWORK_ACL로 별도 덤프하고, TO-BE에서 사용자/역할 생성 후 import한다. Wallet OS 파일은 Data Pump에 포함되지 않으므로 별도 복사한다.
  - 검증 항목: Export 및 Import 로그에서 NETWORK_ACL, NACL$_ACE, NACL$_HOST, NACL$_WALLET 처리를 확인하고 AS-IS/TO-BE의 DBA_HOST_ACLS, DBA_HOST_ACES, DBA_WALLET_ACLS, DBA_WALLET_ACES 결과를 비교한다.
- [2026-07-13 15:25:52] Oracle Incident - execution
  - 질문 맥락: UTL_HTTP.SET_WALLET에 Wallet 비밀번호를 평문으로 지정하고 UTL_HTTP.REQUEST로 HTTPS SOAP URL을 조회하는 테스트의 문제점 문의
  - 에러 메시지: 현재 ORA 오류 없음. 보안 및 검증 적합성 점검
  - 원인 가설: SET_WALLET은 현재 DB 세션의 HTTPS 요청용 Wallet을 설정하므로 영구 DB 변경은 없지만, 평문 비밀번호가 SQL 클라이언트 기록과 SQL 커서 텍스트 등에 노출될 수 있다. REQUEST는 기본 GET으로 응답 본문 첫 2000바이트를 반환하므로 인증서/TLS의 일부만 확인하며 실제 SOAP POST, 인증 헤더, 업무 응답을 검증하지 않는다.
  - 권장 조치: 노출된 Wallet 비밀번호를 변경한다. Auto-login Wallet이면 password NULL을 사용하고, 아니면 애플리케이션 드라이버 Bind로 전달한다. 테스트에는 상세 예외와 짧은 Transfer Timeout을 설정하고 HEAD 또는 GET의 HTTP status만 출력하며 응답 본문은 출력하지 않는다. Connection Pool 세션에서는 전역 SET_WALLET 대신 Request Context 사용을 검토한다.
  - 검증 항목: 실제 서비스 계정/PDB에서 ORA-24247, ORA-28759, ORA-29024 없이 HTTP status를 받고, 별도로 실제 SOAP POST와 인증 헤더를 사용한 업무 테스트를 수행한다.
