# Oracle Incident Log - execution


- [2026-06-08 11:01:54] Oracle Incident - execution
  - 질문 맥락: DBA_DATA_FILES/DBA_FREE_SPACE 기반 tablespace 사용률 80% 이상 조회 SQL 수정
  - 에러 메시지: ORA-00904 가능: Y.USED_PER 식별자 부적합
  - 원인 가설: inner query에서 사용률 alias를 USDE_PER로 생성했지만 outer query에서 USED_PER로 참조했고, 사용률 계산식이 *1000으로 되어 있어 퍼센트 값이 잘못 계산됨. DBA_FREE_SPACE inner join 사용 시 free extent가 없는 tablespace가 누락될 수 있음
  - 권장 조치: datafile 총량과 free space 총량을 각각 집계한 뒤 LEFT JOIN하고, NVL(free_mb,0) 및 *100 기준으로 사용률 계산
  - 검증 항목: USED_PER alias 일치, USED(%) 계산식, free space 없는 tablespace 포함 여부, MOUNT 상태가 아닌 OPEN 상태에서 실행
- [2026-06-08 11:05:11] Oracle Incident - execution
  - 질문 맥락: 이미지의 tablespace 사용률 SQL이 틀린 것인지 재확인
  - 에러 메시지: (not provided)
  - 원인 가설: 이미지 SQL은 USED_PER alias와 *100 계산식은 맞아 문법상 실행 가능성이 높지만, DBA_FREE_SPACE inner join 때문에 free extent가 없는 100% 사용 tablespace가 결과에서 누락될 수 있고, autoextend maxbytes 기준 사용률은 반영하지 않음
  - 권장 조치: 실행 가능 여부만 보면 틀렸다고 보기 어렵지만 운영 보고용은 LEFT JOIN/NVL 방식으로 수정하고, autoextend 최대치 기준이 필요하면 DBA_DATA_FILES.MAXBYTES 기준 별도 계산
  - 검증 항목: 100% full tablespace 포함 여부, 현재 할당량 기준과 MAXBYTES 기준 구분, TEMP tablespace 별도 조회 필요 여부
- [2026-06-08 11:19:46] Oracle Incident - execution
  - 질문 맥락: DBA_FREE_SPACE inner join으로 free extent 0인 tablespace가 누락되어도 상관없는지 확인
  - 에러 메시지: (not provided)
  - 원인 가설: 조회 목적이 free space가 남아 있는 tablespace만 확인하는 것이라면 누락 허용 가능하지만, 80% 이상 또는 위험 tablespace 전체 리포트 목적이면 100% full tablespace 누락은 부정확함
  - 권장 조치: 운영 임계치 리포트는 LEFT JOIN/NVL 방식으로 100% full tablespace를 포함하고, 의도적으로 free extent 존재 대상만 보려면 inner join 사용 의도를 명시
  - 검증 항목: 리포트 목적이 free space 존재 대상인지, 사용률 임계치 대상 전체인지 구분
- [2026-06-08 11:29:47] Oracle Incident - execution
  - 질문 맥락: WITH 절로 DBA_DATA_FILES 총량과 DBA_FREE_SPACE 여유량을 집계한 tablespace 사용률 SQL이면 충분한지 확인
  - 에러 메시지: (not provided)
  - 원인 가설: 현재 할당량 기준 permanent tablespace 사용률을 구하는 목적이면 충분하지만, free extent가 없는 tablespace 누락, TEMP tablespace 제외, autoextend MAXBYTES 기준 미반영은 의도에 따라 별도 처리 필요함
  - 권장 조치: 단순 현재 사용률 조회는 해당 SQL 사용 가능. 80% 이상만 보려면 where 조건에 사용률 계산식을 추가하고, 운영 임계치 전체 리포트는 LEFT JOIN/NVL 방식 고려
  - 검증 항목: OPEN 상태 실행, permanent/TEMP 구분, 현재 할당량 기준과 autoextend 최대치 기준 구분
- [2026-06-08 13:33:05] Oracle Incident - execution
  - 질문 맥락: 온라인 중 특정 데이터파일만 삭제하는 방법
  - 에러 메시지: (not provided)
  - 원인 가설: 운영 중 데이터파일 삭제는 OS 파일 삭제가 아니라 ALTER TABLESPACE ... DROP DATAFILE 절차로 수행해야 하며, 대상 데이터파일은 extents가 없는 empty 상태여야 함
  - 권장 조치: DBA_DATA_FILES로 file_id/tablespace를 확인하고 DBA_EXTENTS에서 해당 file_id의 extent 존재 여부를 검증한 뒤, empty이면 ALTER TABLESPACE <tablespace> DROP DATAFILE '<file_name>' 실행. SYSTEM/SYSAUX/UNDO/첫 번째 또는 유일한 데이터파일은 제외
  - 검증 항목: DB OPEN 상태, 대상 file_id에 extents 0건, 첫 번째/유일한 데이터파일 아님, 백업 및 standby 영향 확인
- [2026-06-08 13:40:40] Oracle Incident - execution
  - 질문 맥락: 삭제 대상 데이터파일에 있는 내용을 move하거나 다른 곳으로 넘긴 뒤 삭제할 수 있는지 확인
  - 에러 메시지: (not provided)
  - 원인 가설: 특정 datafile의 extents를 비우기 위해 table/index/LOB/partition segment를 다른 tablespace로 move/rebuild하는 방식은 가능하지만, permanent datafile의 내용을 다른 datafile로 직접 이관하는 단일 명령은 없음
  - 권장 조치: DBA_EXTENTS에서 file_id별 segment 목록을 확인하고 segment 유형별로 ALTER TABLE MOVE, ALTER INDEX REBUILD, ALTER TABLE MOVE LOB, partition move 등을 수행한 뒤 extents 0건 확인 후 DROP DATAFILE 실행
  - 검증 항목: segment 유형별 move 문법, index unusable 여부, LOB/partition 포함 여부, 대상 tablespace 여유 공간, DROP DATAFILE 전 DBA_EXTENTS 0건
