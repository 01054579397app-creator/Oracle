[2026-08-12 16:09 KST]
- 주제: ASMCA silent ADVM 볼륨 생성 명령 무응답 시 로그 확인
- 질문 맥락: grid 사용자로 `asmca -silent -createVolume -volumeName arch_fs -volumeDiskGroup ARCH_HOME -volumeSizeGB 10` 실행 후 명령이 끝나지 않는 상황에서 로그 위치를 확인하려 함.
- 에러 메시지: 출력 없이 멈춘 상태이며 구체적인 ORA/ASMCMD 오류는 아직 확인되지 않음.
- 원인 가설: ASMCA 작업 진행 또는 내부 ASM/Clusterware 호출 대기일 수 있으므로, 먼저 ASMCA 로그와 프로세스 상태를 확인해야 함. 단순히 멈췄다는 사실만으로 원인을 확정할 수 없음.
- 권장 조치: `$ORACLE_BASE/cfgtoollogs/asmca/<hostname>/asmca.log`와 최신 `cfgtoollogs/asmca` 파일을 확인하고, 별도 터미널에서 ASMCA 및 자식 프로세스와 ASM alert log를 점검함.
- 검증 항목: `asmcmd volinfo -G ARCH_HOME arch_fs`, `asmcmd volinfo --all`, `ps -ef | grep asmca`로 생성 여부와 실행 상태를 확인함.

[2026-08-12 16:18 KST] 추가 업데이트
- 주제: 9GB ASMCA 재시도 실패의 실제 원인 확정
- 질문 맥락: 볼륨 크기를 9GB로 낮춰 동일한 `asmca -silent -createVolume` 명령을 실행했지만 다시 멈추고 오류 로그 위치를 찾지 못함.
- 에러 메시지: `/log/grid/cfgtoollogs/asmca/sysError.log`에 `java.lang.NullPointerException at USMVolumeManager.volumeExists`가 기록됨. `acfsdriverstate supported`는 `ACFS-9459: ADVM/ACFS is not supported on this OS version: 'unknown'`과 `ACFS-9201: Not Supported`를 반환함.
- 원인: 실행 커널은 OL 9.7 UEK 8 `6.12.0-105.51.5.el9uek.x86_64`이고 설치된 GI/ACFS RU는 19.24임. 현재 ACFS/ADVM 드라이버가 이 커널을 지원 또는 인식하지 못해 로드되지 않았으며, ASMCA 19.24가 이를 정상 오류로 출력하지 못하고 볼륨 존재 검사 단계에서 NPE를 발생시킴.
- 권장 조치: 현재 명령은 중단하고 볼륨을 재시도하지 않음. MOS 1369107.1에서 해당 커널 지원 RU를 확인해 GI/ACFS를 업데이트하거나, 점검 시간에 양 RAC 노드를 검증된 지원 커널 조합으로 맞춤. 변경 후 두 노드에서 `acfsdriverstate supported`, `installed`, `loaded`가 정상인지 먼저 확인함.
- 검증 항목: 9GB 실행 로그는 `/log/grid/cfgtoollogs/asmca/asmca-260812PM041402.log`; 해당 PID 155527이 이 로그와 `sysError.log`를 열고 있었음. `v$asm_volume`은 0건이고 `asmcmd volinfo --all`도 `no volumes found`이므로 볼륨은 생성되지 않음.

[2026-08-12 16:25 KST] 추가 업데이트
- 주제: Oracle Linux 9 지원표와 UEK 8 ACFS 드라이버 인증 범위 구분
- 질문 맥락: Oracle 문서에 19.21부터 Oracle Linux 9.2 이상 호환 커널을 지원한다고 되어 있으므로 현재 OL 9.7에서도 동작해야 하는지 확인함.
- 판단: OS 계열 지원 조건은 충족하지만, `호환 커널`은 앞으로 나오는 모든 UEK 메이저 버전을 과거 RU 바이너리가 자동 지원한다는 뜻이 아님. UEK 8은 2025년에 나온 커널 ABI이고 현재 ACFS RU 19.24는 2024년 패치이므로 현재 드라이버가 UEK 8 6.12를 인식하지 못함.
- 실측 근거: `/sw/grid/bin/acfsdriverstate supported`가 `ACFS-9459`, `Not Supported`; `installed`와 `loaded`도 false를 반환함. 따라서 현재 조합에서는 ASMCA 볼륨 생성이 불가능함.
- 권장 조치: MOS 1369107.1에서 커널 `6.12.0-105.51.5.el9uek.x86_64`을 지원하는 정확한 GI/ACFS RU를 확인하고 양 RAC 노드를 같은 조합으로 패치함. 이후 ADVM 호환 속성과 볼륨 생성을 진행함.

[2026-08-12 16:45 KST] 추가 업데이트 및 정정
- 주제: ACFS Interim Patch 38609543 적용 효과와 RAC 노드 불일치 확인
- 사용자 확인: `p38609543_1930000ACFSRU_Linux-x86-64.zip`을 OPatch로 적용하자 ADVM/ACFS 드라이버가 정상화됨.
- 패치 메타데이터: Patch 38609543, Grid Infrastructure 19c용 ACFS Interim Patch, 2026-03-31 생성. README의 대표 버그 설명은 OL9.7/RHEL9.7 RHCK 5.14.0-611.5.1 이상 지원임.
- 실측 결과: nphpagedb1은 여전히 UEK 8 `6.12.0-105.51.5.el9uek.x86_64`로 실행 중이지만 패치 38609543 적용 후 `acfsdriverstate supported/installed/loaded`가 모두 정상이고 oracleacfs/oracleadvm/oracleoks 모듈이 로드됨. 따라서 이 환경에서는 해당 interim patch가 UEK 8 문제도 실제 해결함. 이전에 이 패치가 UEK 8에는 적용되지 않을 것처럼 판단한 부분을 정정함.
- RAC 위험: nphpagedb2에는 38609543이 없고 ACFS RU 19.24만 있으며, 동일 UEK 8에서 `ACFS-9459 Not Supported`, installed=false, loaded=false 상태임. 양 노드의 ACFS/ADVM 패치와 드라이버 상태가 불일치함.
- 후속 조치: nphpagedb2에도 검증된 동일 패치를 정식 절차로 적용하고 두 노드에서 patch inventory와 driver state를 일치시킨 뒤 볼륨을 생성함. 현재 ARCH_HOME에는 `compatible.advm` 속성이 없고 볼륨도 아직 생성되지 않음.

[2026-08-12 16:55 KST] 추가 업데이트
- 주제: Patch 38609543의 UEK 8 공식 근거 확인
- 질문 맥락: 공개 Oracle 문서와 패치 외부 README에는 38609543이 UEK 8에서 동작한다는 설명이 보이지 않는다는 지적.
- 확인 결과: 외부 `README.txt`는 base bug 38609543의 OL9.7/RHEL9.7 RHCK 지원만 표시하지만, 패치 내부 `38609543/38609543/etc/config/inventory.xml`의 누적 수정 목록에는 Bug 37423375 `OL9 - ACFS - UEK8 KERNEL SUPPORT KERNEL 6.12.0-0.20.20 AND LATER UEK8 KERNELS`가 명시되어 있음.
- 패치 파일 근거: 번들에 `files/usm/install/Oracle/EL9UEK/x86_64/6.12.0-0.20.20` 아래 oracleacfs, oracleadvm, oracleoks 모듈이 실제 포함되어 있음.
- 결론: 공개 일반 문서가 아니라 Oracle ACFS 누적 패치의 inventory 메타데이터가 UEK 8 지원 근거임. 현재 커널 6.12.0-105는 해당 문구의 later UEK8 kernels에 해당하며, 실제 드라이버 판정과 로드 결과도 이를 확인함.

[2026-08-12 17:08 KST] 추가 업데이트
- 주제: Patch 38609543 적용 안전성과 향후 OL 9.7 커널 범위
- 패치 성격: bundle.xml 기준 Oracle Database 19.30.0.0.0 ACFSRU이며 최소 OPatch 12.2.0.1.48을 요구함. nphpagedb1의 OPatch는 12.2.0.1.51임.
- 현재 커널 검증: `acfsdriverstate supported -k kernel-uek-core-6.12.0-105.51.5.el9uek.x86_64 -v` 실행 결과 ACFS-9200 Supported 및 패치 내 6.12.0-0.20.20 ACFS RPM과 현재 커널의 호환성이 확인됨.
- 범위 판단: 패치 inventory는 UEK 8 6.12.0-0.20.20 이상 UEK8 커널과 OL9.7/RHEL9.7 RHCK 5.14.0-611.5.1 이상을 명시하지만, 임의의 커스텀 커널, 향후 UEK 메이저 버전, 향후 OS 마이너 버전 전체를 보장하지 않음.
- 운영 원칙: 새 커널 설치 또는 재부팅 전에 `acfsdriverstate supported -k <설치된 커널 패키지명 또는 RPM 경로> -v`로 개별 검증하고 MOS 1369107.1의 제외/알려진 이슈를 확인함. RAC 모든 노드의 ACFS RU와 드라이버 상태를 동일하게 유지함.

[2026-08-12 17:15 KST] 최종 성공 확인
- 실행 결과: `asmca -silent -createVolume -volumeName arch_fs -volumeDiskGroup ARCH_HOME -volumeSizeGB 9`가 2026-08-12 16:54:45 KST에 정상 완료됨. 최신 로그는 `/log/grid/cfgtoollogs/asmca/asmca-260812PM045419.log`임.
- 볼륨: ARCH_HOME/ARCH_FS, `/dev/asm/arch_fs-173`, ENABLED, 9216MB, UNPROT, stripe columns 8, stripe width 1024K.
- 디스크그룹 속성: `compatible.advm=19.0.0.0.0`, `compatible.asm=19.0.0.0.0`, `compatible.rdbms=10.1.0.0.0`.
- RAC 검증: nphpagedb1과 nphpagedb2 모두 Patch 38609543 적용, ACFS supported/installed/loaded 정상. `/dev/asm/arch_fs-173` 장치가 두 노드에 존재하고 `ora.ARCH_HOME.ARCH_FS.advm`이 양 노드에서 ONLINE/STABLE임.
- 결론: 최초 ASMCA NPE/무응답은 ACFS/ADVM 드라이버 미지원 상태에서 발생했으며, ACFSRU 19.30 Patch 38609543을 양 노드에 적용한 후 동일 명령이 정상 완료됨.

[2026-08-12 17:22 KST] ACFS 등록 및 마운트 확인
- 생성 메시지: DBT-30092로 `/dev/asm/arch_fs-173`에 ACFS 생성 성공. 생성된 `/log/grid/cfgtoollogs/asmca/scripts/acfs_script.sh`는 `srvctl add filesystem -d /dev/asm/arch_fs-173 -m /arch -autostart ALWAYS` 후 파일시스템을 시작하는 내용임.
- 현재 상태: 스크립트 실행 결과가 이미 반영되어 `ora.arch_home.arch_fs.acfs` Clusterware 리소스가 등록됨. `/arch`는 nphpagedb1과 nphpagedb2 모두 `/dev/asm/arch_fs-173`로 마운트됨.
- 용량/권한: ACFS 9.0G, 사용 568M, 가용 8.5G, `/arch` 권한 775, 소유자 root:root.
- 검증: `srvctl status filesystem -d /dev/asm/arch_fs-173`에서 양 노드 마운트 확인. 등록 스크립트를 다시 실행할 필요 없음.

[2026-08-12 17:30 KST] 추가 업데이트
- 질문: 생성된 `acfs_script.sh`를 반드시 root로 실행해야 하는지 확인.
- 결론: 해당 스크립트는 `/sw/grid/bin/srvctl add filesystem`으로 Clusterware 파일시스템 리소스를 등록하므로 Linux/Unix에서는 root 실행이 필요함. 이어지는 마운트와 `/arch` 권한 변경도 privileged 작업임.
- 권한 구분: 최초 `srvctl add filesystem` 등록은 root가 수행해야 하지만, 등록 시 `-user <사용자>`를 지정하면 이후 해당 사용자가 `srvctl start/stop filesystem`을 수행하도록 위임할 수 있음. 현재 자동 생성 스크립트에는 `-user`가 없어 root만 시작/중지할 수 있는 구성임.
- 현재 상태: 이미 리소스 등록과 양 노드 마운트가 완료되었으므로 스크립트를 다시 실행할 필요 없음.

[2026-08-12 17:42 KST] 추가 업데이트
- 질문: DB 파일용 ACFS는 ASMCA 대신 volcreate 또는 ALTER DISKGROUP ADD VOLUME으로 생성해야 stripe columns가 1이 된다는 주장 검증.
- 공식 문서 결론: 해당 주장은 19c 기준으로 틀림. Oracle은 12.1.0.2 이상에서 생성되는 볼륨의 DB 파일용 고성능 기본 구성을 stripe columns=8, stripe width=1MB로 명시함. 12.1.0.2 이전에 생성된 볼륨에 대해서만 columns=1 또는 columns=8/width=1MB를 권고함.
- 도구 기본값: ASMCMD volcreate도 `--column` 생략 시 기본값은 8이고 `--width` 기본값은 1MB이므로, volcreate를 사용하면 자동으로 columns=1이 된다는 설명도 사실이 아님. SQL도 STRIPE_COLUMNS를 명시적으로 지정할 수 있을 뿐 ASMCA 대신 SQL을 써야 1이 되는 것은 아님.
- 현재 볼륨: ARCH_FS는 stripe columns=8, stripe width=1024K로 Oracle 19c의 고성능 기본 구성과 정확히 일치함. 해당 이유로 삭제 또는 재생성할 필요 없음.
- 추가 조건: 실제 데이터파일을 ACFS에 저장하는 경우 `FILESYSTEMIO_OPTIONS=SETALL`과 DB 리소스의 ACFS 의존성도 별도로 구성해야 함.

[2026-08-12 17:55 KST] 추가 업데이트
- 질문: `srvctl add filesystem ... -user ... -mtowner/-mtgroup/-mtperm` 및 `srvctl start filesystem`을 별도로 수행해야 하는지 확인.
- 현재 상태: ASMCA 생성 스크립트가 이미 filesystem 리소스 add/start를 완료했으므로 다시 add/start하면 안 됨. 현재 리소스는 양 노드 ONLINE이고 autostart ALWAYS임.
- 옵션 의미: `-user oracle`은 oracle 계정에 srvctl start/stop 즉 마운트/언마운트 관리 권한을 주는 선택 옵션임. DB 프로세스의 파일 쓰기 권한과는 별개임. 19c 현재 구문은 `-mountowner`, `-mountgroup`, `-mountperm`이며 과거 예제의 `-mtowner`, `-mtgroup`, `-mtperm`과 이름이 다를 수 있음.
- 실제 권한 검사: `/arch`는 root:root 775이고 ACL 추가 항목이 없으며 `runuser -u oracle -- test -w /arch` 결과 ORACLE_NOT_WRITABLE임.
- 권장: 아카이브 목적지로 쓸 경우 기존 리소스를 제거/재등록하지 말고 root로 `srvctl modify filesystem -device /dev/asm/arch_fs-173 -mountowner oracle -mountgroup oinstall -mountperm 775`를 설정함. 온라인 변경은 다음 재시작 시 반영되므로 즉시 사용하려면 현재 마운트에 `chown oracle:oinstall /arch; chmod 775 /arch`도 적용함. `-user oracle`은 oracle이 srvctl start/stop을 해야 할 때만 추가함.
