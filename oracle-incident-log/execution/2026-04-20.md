# Oracle Incident Log - execution

- [2026-04-20 10:50:00] Oracle Incident - execution
  - 질문 맥락: `Running as user grid: /sw/grid/bin/cluutil -ckpt -oraclebase /log/grid -writeckpt -name ROOTCRS_PREPATCH -pname NONROLLING -pvalue 0` 문구의 의미 문의
  - 에러 메시지: (not provided)
  - 원인 가설: 이 호출은 실제 바이너리 패치 적용 명령이 아니라 Oracle Grid Infrastructure `rootcrs`/`prepatch` 흐름에서 상태를 남기기 위한 체크포인트 기록이다. `cluutil -ckpt -writeckpt` 는 Oracle base 경로 아래에 체크포인트를 저장하고, `ROOTCRS_PREPATCH` 이름으로 `NONROLLING=0` 값을 기록해 현재 prepatch 세션이 nonrolling 모드가 아니라는 점을 표시한다. 즉 `0` 은 false 의미에 가깝고, rolling 또는 기본 패치 흐름으로 해석하는 용도다.
  - 권장 조치: 답변에서는 각 옵션을 나눠 설명한다. `Running as user grid` 는 OS 사용자 `grid` 로 실행됐다는 뜻이고, `-oraclebase /log/grid` 는 체크포인트 저장 기준 경로, `-name ROOTCRS_PREPATCH` 는 prepatch 단계 식별자, `-pname NONROLLING -pvalue 0` 는 nonrolling 여부를 0(false) 로 저장한다는 뜻이다. 이 값은 이후 `postpatch`, resume, mode 일관성 확인에 사용될 수 있으므로 단독 에러로 보지 않는다.
  - 검증 항목: 같은 로그에서 직전/직후에 `rootcrs.sh -prepatch` 또는 `CLSRSC` 관련 문구가 있는지, 이후 `ROOTCRS_POSTPATCH` 또는 mode/session 확인 로그가 이어지는지, nonrolling 실행이었다면 동일 위치에 `NONROLLING=1` 에 해당하는 흔적이 있는지 점검한다.
  - 추가 업데이트: 사용자는 패치 절차 해석보다 명령어 자체 의미만 원한다고 재확인했다. 따라서 이 줄은 `cluutil` 이 `ROOTCRS_PREPATCH` 체크포인트에 `NONROLLING=0` 속성을 쓰는 명령으로 단순 해석하면 된다. 즉 "prepatch 상태 정보를 기록한다" 가 핵심이며, 옵션별 의미는 `-ckpt` 체크포인트 기능, `-writeckpt` 체크포인트 쓰기, `-name ROOTCRS_PREPATCH` 체크포인트 이름, `-pname NONROLLING` 속성명, `-pvalue 0` 속성값(0=false)이다.
  - 추가 업데이트: 사용자가 `prmsdb2` 기준 `rootcrs.sh -prepatch` 와 `rootcrs.sh -postpatch` 로그를 상세 주석과 함께 정리했다. 해석의 핵심은 1) prepatch 에서 rolling patch mode 전환, CRS/TFA 정지, GI home unlock, postpatch용 checkpoint 초기화까지 수행하고 2) postpatch 에서 prepatch 성공 여부와 `NONROLLING=0` metadata 를 재확인한 뒤 OHASD service 갱신, 로컬 OLR patch 반영, GI stack 기동, OCR patch 반영, rolling patch 종료 시도, OLR backup, TFA 후속 작업, `ROOTCRS_POSTPATCH=SUCCESS` 마감 순으로 진행된다는 점이다. `CRS-1162` 로 rolling patch 종료가 거부된 부분도 아직 `prmsdb1` 패치 레벨이 `prmsdb2` 와 같지 않아서 발생한 정상적인 중간 상태로 정리했다.
  - 추가 업데이트: 표현 하나만 보정하면 `Install node: prmsdb1` 은 "로컬 노드" 뜻이 아니라 install node 또는 기준 노드를 가리키고, 현재 작업 노드는 이어지는 `Current node is not the install node` 문구를 통해 `prmsdb2` 임을 나타낸다. 그 외 `FIRSTNODE_TOPATCH`, `NODENAME_LIST`, `isLastNode`, `DESTCRSHOME=null`, `NORESTART=0`, `RELEASEPATCH=2480431073` 같은 checkpoint/property 해석은 작업 순서 이해에 유효하다.
  - 추가 업데이트: 사용자가 "여기에 간단하게만 정리"를 요청했다. 요약하면 `prmsdb2` 에서 `prepatch` 는 rolling patch mode 전환 후 CRS/TFA 를 내리고 GI home 을 unlock 한 단계이고, `postpatch` 는 patch 반영 뒤 OHASD/OLR/OCR 을 갱신하고 GI 를 다시 올린 단계다. 다만 `prmsdb1` 이 아직 같은 patch level 이 아니어서 cluster 는 `ROLLING PATCH` 상태를 유지했고, 이는 다음 노드 패치 전의 정상 중간 상태다.
