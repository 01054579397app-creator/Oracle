# Oracle Incident Log - execution


- [2026-08-13 08:57:42] Oracle Incident - execution
  - 질문 맥락: TAF 및 TAC 구성 테스트 프로그램의 구조, 실행 방법, 결과 판독법 분석
  - 에러 메시지: 실행 요청은 하지 않았으며 정적 분석 결과: TAC_EXPLICIT_TX.exe가 COMMIT 성공 문구를 출력하지만 실제 Commit 호출이 없음
  - 원인 가설: 두 프로그램은 ODP.NET Unmanaged Driver 19.28 기반이다. ConnectionPool 프로그램은 3초마다 새 논리 연결로 인스턴스명을 조회하여 풀/FAN 재접속을 관찰한다. EXPLICIT_TX 프로그램은 20건 DML 후 장애 유도 대기 구간을 제공하지만 Commit 호출이 없어 연결 종료 시 미커밋 로컬 트랜잭션이 롤백된다.
  - 권장 조치: ODAC19.28 odp.net4와 기본 클라이언트를 관리자 권한으로 설치하고 C:\trace를 생성한다. 두 EXE별 config의 호스트, 포트, 서비스명, 최소권한 테스트 계정을 수정한다. TAC용 서비스는 FAILOVER_TYPE=AUTO, COMMIT_OUTCOME=TRUE, NOTIFICATION=TRUE 등 서버 속성을 검증한다. tac_test 테이블을 준비한 뒤 ConnectionPool은 인스턴스 전환을 관찰한다. EXPLICIT_TX는 현 바이너리로 커밋 성공을 검증할 수 없으므로 Commit 구현 후 사용한다.
  - 검증 항목: srvctl config service 및 ALL_SERVICES에서 서비스 속성을 확인한다. ConnectionPool 출력의 INSTANCE_NAME 전환과 오류 여부를 확인한다. EXPLICIT_TX는 실행 전후 별도 세션에서 tac_test의 COUNT, COUNT(DISTINCT id), MIN/MAX(id)를 조회하고 ODP.NET trace를 확인한다. 현재 PC는 .NET Framework 4.8.1 이상이나 ODAC 레지스트리 등록과 C:\trace가 없다.
- [2026-08-13 09:00:34] Oracle Incident - execution
  - 질문 맥락: TAF/TAC 지식이 전혀 없는 상태에서 기존 배포 프로그램을 이해하고 최종적으로 같은 프로그램을 직접 개발·빌드·배포하기 위한 학습 방향 요청
  - 에러 메시지: 현재 배포본에는 C# 원본 소스가 없고 EXE, PDB, config, Oracle.DataAccess.dll만 존재한다. TAC_EXPLICIT_TX 바이너리는 Commit 호출이 없어 학습용 기준 구현으로 그대로 사용할 수 없다.
  - 원인 가설: 학습자는 Oracle RAC 서비스, TAF/FAN/AC/TAC의 역할 구분부터 ODP.NET 연결·풀·트랜잭션, C# 콘솔 프로그램, .NET Framework 빌드, ODAC Xcopy 배포 순서로 익혀야 한다. 기존 바이너리는 분석 대상이며 최종 프로그램은 새 소스 프로젝트로 재작성하는 것이 안전하다.
  - 권장 조치: 1단계 용어와 전체 흐름, 2단계 기존 ConnectionPool 프로그램 실행, 3단계 최소 C# 조회 프로그램 작성, 4단계 Connection Pool/FAN 관찰 기능 추가, 5단계 명시적 트랜잭션과 실제 Commit 및 검증 구현, 6단계 RAC 서비스 재배치 테스트, 7단계 Release 빌드와 배포 패키지 작성 순으로 진행한다.
  - 검증 항목: 각 단계마다 코드와 실제 출력이 일치하는지 확인한다. 서버에서는 DBA_SERVICES 및 srvctl로 서비스 속성을 확인한다. 트랜잭션 테스트는 별도 세션에서 총 건수, 중복 ID, COMMIT 결과를 검증한다. 최종 배포물에는 EXE, EXE.config, 필요한 ODP.NET 구성요소, README와 테스트 체크리스트를 포함하되 운영 계정 비밀번호는 포함하지 않는다.
- [2026-08-13 09:11:39] Oracle Incident - execution
  - 질문 맥락: TAF/TAC 프로그램 학습을 한 단계씩 진행하며 첫 대상으로 TAC_ConnectionPool.exe.config의 역할부터 설명 요청
  - 에러 메시지: (없음)
  - 원인 가설: 초보 학습 단계에서는 접속 문자열의 세부 옵션을 한꺼번에 설명하기보다 EXE와 EXE.config의 이름 관계, 자동 로딩 시점, XML 최상위 구조를 먼저 이해해야 한다.
  - 권장 조치: 첫 단계에서는 TAC_ConnectionPool.exe.config가 .NET Framework 실행 설정 파일이며 EXE와 같은 폴더 및 같은 기본 이름을 유지해야 한다는 점만 학습한다. 다음 단계에서 connectionStrings 블록으로 이동한다.
  - 검증 항목: TAC_ConnectionPool.exe와 TAC_ConnectionPool.exe.config가 같은 폴더에 있고 파일 기본 이름이 일치하는지 확인한다. 설정 파일 1~4행의 XML 및 configuration 요소를 식별한다.
- [2026-08-13 09:13:03] Oracle Incident - execution
  - 질문 맥락: TAC_ConnectionPool.exe.config 단계별 학습 2단계: connectionStrings, add, name, providerName 설명
  - 에러 메시지: (없음)
  - 원인 가설: connectionStrings는 DB 접속 설정들의 보관 영역이고 add 요소 하나가 이름을 가진 접속 설정 한 건을 등록한다. 프로그램은 PTSDB라는 설정 키로 해당 항목을 찾고 Oracle.DataAccess.Client 공급자를 사용한다.
  - 권장 조치: connectionStrings 블록과 name=PTSDB, providerName=Oracle.DataAccess.Client의 역할만 설명한다. 실제 connectionString 내부 항목은 다음 단계에서 다룬다.
  - 검증 항목: 설정 파일 4~6행에서 connectionStrings, add, name, providerName을 확인하고 PTSDB가 실제 Oracle 서비스명이 아닌 애플리케이션 내부 설정 키임을 구분한다.
- [2026-08-13 09:46:16] Oracle Incident - execution
  - 질문 맥락: TAC_ConnectionPool.exe를 그냥 실행하면 필요한 ODAC도 자동 설치되는지 문의
  - 에러 메시지: OraOps19.dll 미설치 상태에서는 EXE 실행 시 DllNotFoundException 발생
  - 원인 가설: TAC_ConnectionPool.exe는 ODAC 설치 기능이 없는 일반 .NET 실행 파일이며 시스템에 ODP.NET Unmanaged Driver와 Oracle Client가 이미 설치·등록되어 있다고 가정한다. ZIP 압축 해제도 설치가 아니다.
  - 권장 조치: 최초 1회 관리자 권한 CMD에서 번들 install.bat에 필수 인자를 전달해 ODAC를 설치한 뒤 EXE를 실행한다. install.bat 자체를 인자 없이 더블클릭하지 않는다.
  - 검증 항목: ODP.NET 레지스트리 DllPath와 OraOps19.dll/oci.dll 설치를 확인한 후 새 CMD에서 EXE를 실행한다.
- [2026-08-13 09:47:28] Oracle Incident - execution
  - 질문 맥락: ODAC install.bat을 더블클릭하면 왜 설치되지 않는지 문의
  - 에러 메시지: install.bat은 component_name, oracle_home_path, oracle_home_name 필수 인자가 비어 있으면 Usage로 이동하고 종료한다.
  - 원인 가설: install.bat은 GUI 설치 프로그램이 아니라 명령행 배치 스크립트이므로 Windows 탐색기에서 더블클릭하면 인자 없이 실행된다. 설치 대상과 구성 방식을 결정할 수 없어 사용법만 출력하고 종료한다.
  - 권장 조치: 관리자 CMD에서 필수 인자를 지정해 실행하거나, 필요하면 해당 환경의 고정 인자를 넣은 관리자용 실행 래퍼 BAT를 별도로 만든다.
  - 검증 항목: install.bat의 ParseArgs 및 Usage 분기와 install.log 생성 여부를 확인한다.
- [2026-08-13 09:53:31] Oracle Incident - execution
  - 질문 맥락: ODAC 영구 설치 명령 install.bat odp.net4 C:\\Oracle\\ODAC1928 ODAC1928 true true 실행 확인
  - 에러 메시지: (아직 실행 결과 없음)
  - 원인 가설: 관리자 권한 CMD에서 ODAC19.28Xcopy_x64 원본 디렉터리를 현재 경로로 둔 경우 올바른 명령이다.
  - 권장 조치: 명령 실행 후 콘솔이 종료되지 않은 상태에서 출력과 C:\\Oracle\\ODAC1928\\install.log 생성 여부를 확인한다.
  - 검증 항목: 설치 완료 메시지, install.log, bin\\OraOps19.dll 및 oci.dll 존재를 확인한다.
- [2026-08-13 10:09:47] Oracle Incident - execution
  - 질문 맥락: TAC_ConnectionPool.exe가 약 3초 간격으로 DMHOME1 인스턴스를 계속 출력하며 실행 중
  - 에러 메시지: (없음)
  - 원인 가설: ODAC 로딩, SCAN 192.168.40.195:11521 접속, dmhome 서비스 인증, V$INSTANCE 조회 및 Connection Pool의 연결 대여/반환이 정상 동작한다. 출력이 계속 DMHOME1인 것은 현재 풀에서 반환되는 연결이 DMHOME1에 수립된 상태를 의미한다.
  - 권장 조치: 현재 출력을 장애 전 정상 기준선으로 보존한다. 다음 단계에서 TAC용 서비스 구성을 확인한 뒤 승인된 서비스 재배치 또는 인스턴스 장애를 수행하여 DMHOME2 전환과 오류 노출 여부를 관찰한다.
  - 검증 항목: 장애 전에는 3초 간격으로 DMHOME1 출력이 지속되고 Oracle error가 없어야 한다. 이 결과만으로 TAC replay 성공을 판정하지 않는다.
- [2026-08-13 10:18:26] Oracle Incident - execution
  - 질문 맥락: TAC_ConnectionPool 실행 중 DB에서 현재 Connection Pool 세션 수와 인스턴스 분포 확인 요청
  - 에러 메시지: (없음)
  - 원인 가설: GV$SESSION에서 SERVICE_NAME=DMHOME, PROGRAM=TAC_ConnectionPool.exe, USERNAME=SYSTEM으로 총 20개 INACTIVE 세션이 확인되어 config의 Min Pool Size=20이 실제 적용됐다.
  - 권장 조치: GV$SESSION을 서비스명, 프로그램명, 사용자명으로 필터링해 인스턴스별 ACTIVE/INACTIVE 세션을 집계한다. 테스트 중에는 동일 SQL을 반복 조회하여 장애 전후 분포 변화를 비교한다.
  - 검증 항목: 조회 시점에 20개 세션 모두 INST_ID=2였으며 GV$INSTANCE 매핑상 INST_ID 1=DMHOME1, INST_ID 2=DMHOME2다. 사용자 콘솔의 이전 DMHOME1 출력과 조회 시점이 다를 수 있으므로 최신 프로그램 출력을 함께 비교한다.
- [2026-08-13 10:26:27] Oracle Incident - execution
  - 질문 맥락: Min Pool Size=20인데 RAC 두 노드에 각각 20개씩 붙은 것처럼 보이는 이유 문의
  - 에러 메시지: (없음)
  - 원인 가설: 현재 실측상 Windows TAC_ConnectionPool.exe 프로세스는 PID 72812 하나이고 DB 세션은 총 20개이며 모두 DMHOME2에 있다. 두 노드에서 GV$SESSION을 각각 조회하면 GV$가 RAC 전체 뷰이므로 동일한 20개가 양쪽에서 반복 표시되어 각 노드에 20개씩인 것으로 오해할 수 있다.
  - 권장 조치: 인스턴스별 분포는 GV$SESSION의 INST_ID를 GV$INSTANCE와 조인해 한 번만 집계한다. 로컬 인스턴스만 확인하려면 V$SESSION을 사용한다. 별도 EXE 프로세스가 여러 개일 때만 Pool당 Min Pool Size가 적용되어 20개씩 추가될 수 있다.
  - 검증 항목: 현재 Get-CimInstance 결과 프로세스 1개, GV$SESSION 집계 결과 DMHOME2 INACTIVE 20개, DMHOME1 0개를 확인했다.
- [2026-08-13 12:06:12] Oracle Incident - execution
  - 질문 맥락: SYSTEM.TAC_TEST 테이블에 PL/SQL로 1초마다 한 번씩 INSERT하는 방법 요청
  - 에러 메시지: (없음)
  - 원인 가설: 단일 테스트 세션에서 현재 MAX(ID)+1을 시작값으로 잡고 DBMS_SESSION.SLEEP(1)을 사용하면 기존 PK 충돌을 피하면서 초당 1건 입력할 수 있다. 매 행 COMMIT 여부에 따라 장애 시 보존 범위가 달라진다.
  - 권장 조치: 무한 루프 PL/SQL 블록에서 INSERT 후 COMMIT하고 DBMS_SESSION.SLEEP(1)을 호출한다. SQL*Plus 또는 SQL Developer에서 실행하고 중지는 Ctrl+C 또는 실행 취소로 한다. 실제 TAF/TAC 트랜잭션 검증에서는 매행 COMMIT 방식과 하나의 장기 트랜잭션 방식을 구분한다.
  - 검증 항목: 별도 세션에서 SYSTEM.TAC_TEST의 최근 ID와 생성값을 조회해 약 1초 간격 증가를 확인한다. 중지 후 마지막 COMMIT 행까지 남는지 확인한다.
- [2026-08-13 12:07:40] Oracle Incident - execution
  - 질문 맥락: 1초마다 INSERT하면서 현재 RAC 인스턴스명도 화면과 테이블에 보이도록 PL/SQL 수정 요청
  - 에러 메시지: (없음)
  - 원인 가설: SYS_CONTEXT(USERENV, INSTANCE_NAME)으로 현재 세션의 인스턴스명을 얻어 TAC_TEST.DATA에 저장하고 DBMS_OUTPUT으로 출력할 수 있다. 장애로 세션이 끊기면 익명 PL/SQL 블록 자체는 자동으로 다른 인스턴스에서 계속되지 않는다.
  - 권장 조치: 루프마다 현재 INSTANCE_NAME을 조회해 ID와 함께 INSERT, COMMIT, 출력 후 1초 대기한다.
  - 검증 항목: 별도 세션에서 ID, DATA를 조회해 DMHOME1 또는 DMHOME2가 저장되는지 확인한다.
- [2026-08-13 12:08:22] Oracle Incident - execution
  - 질문 맥락: sqlplus here-document 안에서 V$INSTANCE 조회 사이에 ! sleep 1을 넣는 방식이 가능한지 문의
  - 에러 메시지: (없음)
  - 원인 가설: Unix SQL*Plus에서는 HOST sleep 1 또는 ! sleep 1 실행이 가능하다. 다만 unquoted heredoc에서는 shell이 $instance를 변수 치환하므로 quoted EOF 또는 v\$instance가 필요하다.
  - 권장 조치: sqlplus -s / as sysdba << quoted EOF 형식으로 SELECT, HOST sleep 1, SELECT, EXIT를 실행한다.
  - 검증 항목: 두 INSTANCE_NAME 출력 사이에 약 1초 간격이 생기고 V$INSTANCE가 정상 인식되는지 확인한다.
- [2026-08-13 12:13:46] Oracle Incident - execution
  - 질문 맥락: while true 안에서 SQLPlus here-document와 여러 HOST sleep을 반복했더니 Ctrl+C로 종료하기 어려움
  - 에러 메시지: 무한 while이 현재 sqlplus/host sleep 종료 후 새 sqlplus를 즉시 재실행함
  - 원인 가설: SIGINT가 외부 while 전체가 아니라 포그라운드 sqlplus 또는 HOST sleep에 전달되어 자식만 중단되고 부모 shell loop는 계속된다. 또한 기존 스크립트는 명시적 COMMIT과 EXIT가 없고 SYS 계정 및 unqualified seq 사용 위험이 있다.
  - 권장 조치: 현재 세션은 SecureCRT Disconnect로 종료한다. 수정본은 trap으로 INT/TERM 시 전체 shell loop를 종료하고 SQLPlus 한 회당 SELECT, INSERT, COMMIT, EXIT만 수행한 뒤 shell에서 sleep 1한다. WHENEVER SQLERROR/OSERROR로 오류 시 종료 여부를 명시한다.
  - 검증 항목: Ctrl+C 한 번으로 스크립트가 종료되고, 각 반복마다 한 행만 커밋되는지 확인한다. 장애 후 재접속 테스트와 기존 세션 TAF 테스트를 구분한다.
- [2026-08-13 12:17:05] Oracle Incident - execution
  - 질문 맥락: 매초 SQLPlus 새 접속 후 인스턴스 조회와 INSERT를 수행하는 스크립트가 TAC/TAF 테스트로 맞는지 문의
  - 에러 메시지: 현재 방식은 매 반복마다 SQLPlus를 새로 시작하므로 기존 세션 failover 또는 request replay를 검증하지 못함
  - 원인 가설: 이 스크립트는 장애 후 신규 연결 가능성과 SCAN/connect-time failover만 확인한다. TAF SELECT는 하나의 지속 연결과 열린 SELECT cursor가 장애 후 이어져야 하고, TAC는 전용 서비스 FAILOVER_TYPE=AUTO, COMMIT_OUTCOME=TRUE, FAN 활성화 및 지원 드라이버에서 하나의 in-flight request가 오류 없이 replay되어 exactly-once 결과를 내야 한다.
  - 권장 조치: TAF와 TAC를 별도 시나리오로 분리한다. TAF는 클라이언트가 실제 사용하는 FAILOVER_MODE TYPE=SELECT descriptor와 장시간 fetch 프로그램으로 검증한다. TAC는 별도 Clusterware 서비스와 Commit이 실제 구현된 ODP.NET 프로그램으로 검증한다. 기본 DMHOME 서비스와 기존 TAC_EXPLICIT_TX.exe로는 TAC 테스트를 중단한다.
  - 검증 항목: TAF 성공은 동일 프로세스의 열린 조회가 ORA 오류 없이 다른 인스턴스에서 계속되는 것으로 판정한다. TAC 성공은 장애 중 ORA-03113이 사용자에게 노출되지 않고 최종 데이터가 누락/중복 없이 정확히 한 번 커밋되는 것으로 판정한다.
- [2026-08-13 12:21:03] Oracle Incident - execution
  - 질문 맥락: 현재 RAC 환경과 제공된 실행 파일로 TAF 장애 테스트를 어떻게 수행하는지
  - 에러 메시지: (not provided)
  - 원인 가설: TAC_EXPLICIT_TX.exe의 INSERT 반복은 TAF SELECT 시험이 아니며 TAC_ConnectionPool.exe의 반복 신규 연결은 접속 시점 failover만 확인한다. TAF SELECT는 이미 열린 세션의 진행 중 SELECT가 인스턴스 장애 후 다른 인스턴스에서 이어지는지 확인해야 한다.
  - 권장 조치: Windows 클라이언트가 실제 사용하는 connect descriptor에 FAILOVER_MODE TYPE=SELECT METHOD=BASIC를 넣고 접속 세션의 V$SESSION 값을 확인한 다음 장시간 SELECT 도중 해당 인스턴스를 중단하여 SELECT 지속, 인스턴스 변경, FAILED_OVER=YES를 검증한다.
  - 검증 항목: 장애 전 FAILOVER_TYPE=SELECT, FAILOVER_METHOD=BASIC, FAILED_OVER=NO. 장애 후 오류 없이 SELECT가 지속되고 인스턴스가 변경되며 FAILED_OVER=YES인지 확인.
