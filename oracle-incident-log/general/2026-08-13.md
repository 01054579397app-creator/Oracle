# Oracle Incident Log - general


- [2026-08-13 14:23:55] Oracle Incident - general
  - 질문 맥락: 데이터파일 앞부분이 비어 있고 새 테이블만 뒤쪽을 사용하는데 왜 RESIZE가 불가능한가
  - 에러 메시지: 데이터파일 후미에 사용 중인 extent가 있어 목표 크기까지 축소 불가
  - 원인 가설: RESIZE가 파일 내부 블록을 자동 압축하거나 앞쪽으로 이동한다고 오해할 수 있으나, Oracle RESIZE는 후미의 연속된 빈 영역만 절단하며 세그먼트를 재배치하지 않음
  - 권장 조치: TAF_TEST의 OWNER, TABLESPACE_NAME, FILE_ID, extent 시작과 끝 위치를 확인한 후 테이블 MOVE 및 인덱스 재구성 여부를 결정한다. SYSTEM 또는 SYSAUX라면 무작정 이동하지 않는다.
  - 검증 항목: MOVE 후 DBA_EXTENTS의 파일 HWM이 내려갔는지 재조회한 다음 HWM보다 큰 값으로 RESIZE한다.
- [2026-08-13 14:33:41] Oracle Incident - general
  - 질문 맥락: TAF TYPE=SELECT라면 인스턴스 장애 시 진행 중 SELECT가 오류 없이 그대로 조회되어야 하는지
  - 에러 메시지: 진행 중 SELECT가 ORA-03113 등으로 종료된다면 TAF SELECT 성공으로 볼 수 없음
  - 원인 가설: TAF SELECT는 장애 후 새 세션을 만들고 진행 중이던 SELECT 커서를 다시 실행하여 이미 전달된 행 위치까지 건너뛴 후 fetch를 계속한다. 단, 재접속 시간 동안 일시 정지될 수 있고 DML, CTAS, 트랜잭션 replay 기능은 아님.
  - 권장 조치: FAILOVER_MODE TYPE=SELECT METHOD=BASIC인 실제 클라이언트 연결에서 V$SESSION FAILOVER_TYPE=SELECT를 먼저 확인하고, SQLPlus ARRAYSIZE 1 및 PAUSE로 fetch 중단점을 만든 뒤 현재 인스턴스를 중지하여 같은 SELECT가 수동 재실행 없이 계속되는지 확인한다.
  - 검증 항목: 장애 전 SELECT/BASIC/NO, 장애 후 ORA 없이 fetch 계속, 인스턴스 변경, FAILED_OVER=YES를 확인한다.
- [2026-08-13 14:51:28] Oracle Incident - general
  - 질문 맥락: TAF SELECT 시험에서 INSTANCE_NAME을 별도로 확인하면 되는데 왜 진행 중 SELECT 결과에 INSTANCE_NAME을 포함하면 ORA-25401이 발생하는가
  - 에러 메시지: ORA-25401: can not continue fetches
  - 원인 가설: TAF SELECT는 장애 후 같은 SELECT를 같은 SCN으로 재실행하고 이미 전달한 행 수만큼 다시 fetch하여 기존 결과와 비교한 뒤 버린다. V$INSTANCE.INSTANCE_NAME을 결과에 포함하면 노드1 DMHOME1과 노드2 DMHOME2가 첫 행부터 달라 prefix 검증에 실패한다. 별도 조회는 진행 중 커서의 결과가 아니므로 문제없다. object_name만 조회해도 ORDER BY가 없으면 재실행 순서가 달라질 위험이 있다.
  - 권장 조치: 인스턴스명은 SELECT 전후 별도 조회하고, 진행 중 시험 SQL은 SELECT id,object_name FROM sys.taf_test ORDER BY id로 고정한다. 실행 전 동일 세션의 V$SESSION이 SELECT/BASIC/NO인지 확인한다.
  - 검증 항목: 장애 후 ORA 없이 fetch가 이어지고, 완료 후 인스턴스 변경과 FAILED_OVER=YES를 확인한다.
- [2026-08-13 15:22:15] Oracle Incident - general
  - 질문 맥락: 보유 중인 TAC_ConnectionPool.exe와 TAC_EXPLICIT_TX.exe 두 프로그램만으로 TAF와 TAC 테스트가 충분해야 하는지
  - 에러 메시지: (not provided)
  - 원인 가설: 프로그램 두 개로 각각 TAF와 TAC를 시험하는 설계는 가능하지만 현재 빌드는 목적을 충족하지 못함. ConnectionPool 프로그램은 매번 풀에서 연결을 열고 짧은 V$INSTANCE 조회 후 닫으므로 진행 중 SELECT cursor의 TAF 복구를 검증하지 않음. EXPLICIT_TX 프로그램은 BeginTransaction과 Commit 호출이 없고 예외 시 종료하므로 TAC request replay 및 exactly-once commit을 검증하지 않음.
  - 권장 조치: TAF용은 하나의 열린 연결과 DataReader에서 ORDER BY가 있는 장기 SELECT를 계속 fetch하도록 수정. TAC용은 TAC 전용 서비스와 지원 드라이버 설정을 사용하고 명시적 트랜잭션, 실제 COMMIT, 장애 후 결과 및 중복 검증을 구현.
  - 검증 항목: TAF는 ORA 없이 fetch 지속 및 FAILED_OVER=YES. TAC는 장애 중 요청 성공, 실제 COMMIT 성공, 행이 정확히 한 번 반영됐는지 확인.
- [2026-08-13 15:31:40] Oracle Incident - general
  - 질문 맥락: TAF & TAC 구성 테스트 프로그램 폴더 전체를 재검사하여 놓친 실행 파일, 소스, 설정, 트레이스가 있는지 확인
  - 에러 메시지: 기존 설명에서 TAC_ConnectionPool.exe를 TAF 시험용으로 잘못 분류했고 TAC_TAF_테스트_런북.md에도 같은 잘못된 분류가 존재
  - 원인 가설: 폴더명은 TAF & TAC이나 실제 실행 파일은 TAC_ConnectionPool.exe와 TAC_EXPLICIT_TX.exe 두 개뿐이며 둘 다 TAC 보조 시험 성격. TAF 전용 DataReader 장기 fetch 실행 파일은 없음.
  - 권장 조치: TAF는 별도 SQLPlus 스크립트 또는 신규 DataReader 프로그램으로 시험. ConnectionPool은 FAN/풀의 죽은 연결 제거 및 신규 연결 회복 관찰용으로만 판정. EXPLICIT_TX는 BeginTransaction/Commit이 없는 Unmanaged ODP.NET 자동커밋 개별 INSERT replay 시험으로 한정하여 판정하고 런북의 잘못된 TAF 시나리오를 수정해야 함.
  - 검증 항목: 전체 240파일과 숨김 파일 확인. 원본 실행 파일 2개, PDB 2개, config 2개뿐. 바이너리 문자열에서 ConnectionPool은 ExecuteScalar만, EXPLICIT_TX는 ExecuteNonQuery만 확인. ExecuteReader/BeginTransaction/Commit 없음. config 둘 다 Application Continuity=true, SERVICE_NAME=DMHOME이며 TAF FAILOVER_MODE 없음. C:\trace 474파일 약 52MB에서 Replay 문자열 0건, ORA-03113 18건/6파일 확인.
