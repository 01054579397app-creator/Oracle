# Oracle Incident Log - errors


- [2026-07-14 08:55:54] Oracle Incident - errors
  - 질문 맥락: Data Pump에서 Scheduler PROCOBJ 생성 중 SYSTEM.SCHEDULER_JOB_ARGS_TMP 부재와 ORA-39126 발생
  - 에러 메시지: ORA-39083, ORA-00942: SYSTEM.SCHEDULER_JOB_ARGS_TMP table or view does not exist, PLS-00364, ORA-39126 KUPW$WORKER.SEND_TRACE_MSG, ORA-06502 character string buffer too small
  - 원인 가설: Scheduler Job 인수 복원용 내부 스테이징 테이블을 찾지 못해 4개 Job 생성 블록이 실패했다. 동일 오류가 두 번 반복되어 PROCOBJ와 SCHEDULER 메타데이터를 분할 또는 중복 import한 영향 가능성이 높다. ORA-39126은 이 실패 메시지를 처리하던 Data Pump Worker의 2차 내부 오류다.
  - 권장 조치: SYSTEM 테이블을 수동 생성하지 않는다. 대상 Job 존재 여부와 SYSTEM.SCHEDULER_JOB_ARGS_TMP 존재 여부를 확인하고, SQLFILE로 덤프 메타데이터를 점검한다. PROCOBJ/SCHEDULER 중복 import를 제거하여 Scheduler 메타데이터를 한 번만 재실행한다. 깨끗한 단일 재실행에서도 ORA-39126이 재현되면 19.24보다 최신 적용 가능 RU와 Oracle Support 버그를 확인한다.
  - 검증 항목: DBA_SCHEDULER_JOBS와 DBA_SCHEDULER_JOB_ARGS에서 4개 Job 및 인수 확인; import 로그에서 ORA-39083/39126 부재와 Job enabled 상태 확인; 소스/대상 NLS 문자셋 및 한글 주석 표시 확인
- [2026-07-14 11:11:26] Oracle Incident - errors
  - 질문 맥락: MISADM 패키지에서 DB Link 원격 테이블 조회 구문이 ORA-00942로 컴파일 실패하지만 별도 COUNT 조회는 성공함
  - 에러 메시지: PKG_MISCONSTMANGTRGET body line 30, 37, 82: PL/SQL ORA-00942 table or view does not exist
  - 원인 가설: COUNT 조회 세션과 패키지 소유자 MISADM의 보안 컨텍스트가 다르다. MISADM이 해당 private/public DB Link를 동일하게 사용하지 못하거나, 링크가 연결하는 원격 계정의 테이블 SELECT 권한이 역할로만 부여되어 저장 PL/SQL 정적 SQL 컴파일 시 인정되지 않는 경우가 우선 원인이다.
  - 권장 조치: 실제 MISADM 로그인 세션에서 USER와 USER@DL_CONSTERP_REAL 및 두 COUNT를 재검증한다. DBA_DB_LINKS에서 링크 소유자와 원격 사용자 유형을 확인한다. 원격 DB에서 실제 테이블 소유자를 확인한 후 링크 원격 계정에 두 테이블 SELECT를 직접 GRANT하거나 MISADM 소유 링크를 올바르게 생성한다.
  - 검증 항목: MISADM 세션에서 두 원격 COUNT 성공; ALTER PACKAGE MISADM.PKG_MISCONSTMANGTRGET COMPILE BODY 후 DBA_ERRORS 0건; 패키지 실행 시 두 REF CURSOR 정상 반환
- [2026-07-14 11:12:44] Oracle Incident - errors
  - 질문 맥락: 추가 업데이트: DB Link를 못 찾는다면 ORA-02019여야 하는데 패키지 컴파일은 왜 ORA-00942인가
  - 에러 메시지: ORA-00942 versus ORA-02019
  - 원인 가설: ORA-00942는 DB Link 이름 해석과 원격 접속은 성공했지만 원격 보안 컨텍스트에서 객체가 없거나 접근 권한이 없어 이름 해석이 실패했음을 뜻한다. SYS와 MISADM이 동일 이름의 서로 다른 private/public link를 사용하거나 connected/current-user link가 서로 다른 원격 사용자로 연결될 수 있다. 같은 MISADM 직접 조회만 성공하면 원격 권한이 역할로만 부여된 가능성이 높다.
  - 권장 조치: DBA_DB_LINKS에서 DL_CONSTERP_REAL의 SYS/MISADM/PUBLIC 중복 정의와 USERNAME을 확인한다. 실제 SYS 및 MISADM 세션 각각에서 로컬 USER와 USER@link를 비교한다. 원격 DBA가 실제 객체 소유자와 링크 원격 계정의 직접 SELECT 권한을 확인한다.
  - 검증 항목: MISADM 실제 세션과 패키지 컴파일이 동일한 link 정의 및 원격 사용자로 연결되는지 확인; 원격 직접 GRANT 후 패키지 body 컴파일 오류 0건 확인
- [2026-07-14 11:14:21] Oracle Incident - errors
  - 질문 맥락: 추가 업데이트: MISADM 직접 SELECT는 ORA-02019인데 동일 SQL을 포함한 패키지 컴파일은 ORA-00942로 표시됨
  - 에러 메시지: 직접 SQL ORA-02019; PACKAGE BODY 컴파일 PL/SQL ORA-00942
  - 원인 가설: 확정 원인은 MISADM에서 DL_CONSTERP_REAL 연결 설명을 찾지 못하는 것이다. 직접 SQL은 실행 단계에서 ORA-02019를 반환하지만, 패키지의 정적 SQL은 컴파일 단계의 원격 객체 이름 해석에 실패하여 USER_ERRORS에 ORA-00942로 일반화되어 기록된다. 이전 원격 직접 권한 가설은 이 확인 결과에 해당하지 않는다.
  - 권장 조치: DBA_DB_LINKS에서 MISADM 또는 PUBLIC 소유 DL_CONSTERP_REAL 존재 여부와 전체 도메인 이름을 확인한다. 누락 또는 이름 불일치를 수정한 뒤 MISADM 직접 SELECT를 먼저 성공시키고 패키지 body를 재컴파일한다.
  - 검증 항목: MISADM의 SELECT USER FROM DUAL@DL_CONSTERP_REAL 성공; 두 원격 테이블 COUNT 성공; 패키지 body DBA_ERRORS 0건
