# Oracle Incident Log - errors

- [2026-05-08 14:35:00] Oracle Incident - errors
  - 주제: OPatchAuto bootstrap 단계 `perl.zip` 압축 해제 실패
  - 질문 맥락: `opatchauto` 실행 중 `OPATCHAUTO-72083: Performing bootstrap operations failed`가 발생했고, 상세 원인은 `Failed to unzip files on path ./back/38629535/38632161/files/perl.zip`로 표시됨.
  - 에러 메시지: `OPATCHAUTO-72083`, `The bootstrap execution failed because Failed to unzip files on path ./back/38629535/38632161/files/perl.zip`.
  - 원인 가정: 실패 위치가 Grid/DB 패치 적용 전 bootstrap 단계이므로 CRS/rootcrs 체크포인트 실패가 아니라, 패치 staging 디렉터리 안의 `perl.zip`을 OPatchAuto가 풀지 못한 문제로 보는 것이 우선이다. 주요 후보는 패치 파일 손상/불완전 압축해제, staging 경로 권한 문제, 디스크/inode 부족, 백업 또는 보안 프로그램의 파일 잠금, 너무 제한적인 mount 옵션 또는 임시 디렉터리 권한 문제다.
  - 권장 조치: 해당 노드에서 `38629535/38632161/files/perl.zip` 파일 존재 여부와 크기를 확인하고 `unzip -t`로 무결성을 검사한다. 실패하면 패치 zip을 다시 전송/압축해제한다. staging 경로와 임시 경로의 여유 공간, 소유자/권한, 파일 잠금 여부를 확인한 뒤 같은 Oracle/Grid 소유자 계정으로 다시 실행한다.
  - 검증 항목: `ls -l ./back/38629535/38632161/files/perl.zip`, `unzip -t ./back/38629535/38632161/files/perl.zip`, `df -h`, `df -i`, `id`, `ls -ld ./back ./back/38629535 ./back/38629535/38632161/files`, `opatchauto` 재실행 전 `opatchauto*.log`에서 bootstrap 재시도 결과 확인.

- [2026-05-08 14:42:00] 추가 업데이트
  - 주제: OPatchAuto topology 수집 중 `cluvfy comp ssa` 실패
  - 추가 로그: OPatchAuto가 `/sw/grid`가 shared Oracle Home인지 확인하려고 `/sw/grid/bin/cluvfy comp ssa -t software -s /sw/grid/crs/install -n prmsdb1,prmsdb2 -display_status`를 `grid` 계정으로 실행했으나 `oracle.cluster.common.NodeRole. CLSD/ADR initialization failed with return value -1`, `Verification cannot proceed`, `The result of cluvfy command does not contain OVERALL_STATUS String`가 발생함.
  - 원인 가정: 이 부분은 `perl.zip` 압축 해제 실패와 별개의 topology discovery 실패다. `cluvfy`가 정상 결과 문자열을 내지 못해 OPatchAuto가 `/sw/grid`의 shared home 여부를 결정하지 못했고, 그 결과 system instance 생성이 중단됐다. 우선 GI/CRS 상태, `grid` 계정 환경, Oracle Base/ADR 경로 권한, `/sw/grid/cfgtoollogs` 권한, 양 노드 이름 해석 및 노드 간 통신 상태를 확인해야 한다.
  - 권장 조치: `grid` 계정으로 동일한 `cluvfy comp ssa` 명령을 수동 실행해서 같은 오류가 재현되는지 확인한다. 이어서 `crsctl check crs`, `olsnodes -n`, `cluvfy stage -pre patch`, `adrci show homes`, `/sw/grid/log/<node>/client` 또는 `cvutrace.log`를 확인한다. `/sw/grid/cfgtoollogs/opatchautodb`는 로그상 `chown -R grid:oinstall`이 성공했지만, 그 상위 경로와 ADR/log 경로 권한까지 별도로 확인한다.
  - 검증 항목: `su - grid -c '/sw/grid/bin/cluvfy comp ssa -t software -s /sw/grid/crs/install -n prmsdb1,prmsdb2 -display_status'`, `su - grid -c 'crsctl check crs'`, `su - grid -c 'olsnodes -n'`, `su - grid -c 'adrci show homes'`, `ls -ld /sw/grid /sw/grid/cfgtoollogs /sw/grid/cfgtoollogs/opatchautodb`, 각 노드의 `/etc/hosts` 또는 DNS 해석 결과.

- [2026-05-08 14:52:00] 추가 업데이트
  - 주제: `CLSD/ADR initialization failed`의 실제 원인 확인
  - 추가 로그: `crsctl check crs`와 `olsnodes -n` 모두 `DIA-49802: missing read, write, or execute permission on specified ADR home directory [/log/grid/diag/crs/prmsdb1/crs/log]`, `DIA-49801: actual permissions [rwxrwxr-x], expected minimum permissions [rwxrwxrwx] for effective user [grid]`, `Linux-x86_64 Error: 13: Permission denied`를 출력함.
  - 원인 확정: OPatchAuto의 shared home 판단 실패는 GI가 내려가서가 아니라, `grid` 계정이 CRS ADR 로그 디렉터리 `/log/grid/diag/crs/prmsdb1/crs/log`에 필요한 권한을 갖지 못해 CRS 유틸리티의 ADR 초기화가 실패한 것이다. `CRS-4638`, `CRS-4537`, `CRS-4529`, `CRS-4533`는 online으로 출력되므로 Clusterware 자체 상태와 OPatchAuto topology discovery 실패를 분리해서 봐야 한다.
  - 권장 조치: 해당 노드에서 `/log/grid/diag/crs/prmsdb1/crs/log` 및 상위 경로의 owner/group/mode를 확인하고, 우선 문제가 표시된 leaf 디렉터리에 대해 `chmod 777 /log/grid/diag/crs/prmsdb1/crs/log` 또는 사이트 표준에 맞는 owner/group 재정렬을 적용한다. 같은 증상이 prmsdb2에서도 나오면 `/log/grid/diag/crs/prmsdb2/crs/log`도 동일하게 확인한다.
  - 검증 항목: 권한 수정 후 `su - grid -c '/sw/grid/bin/crsctl check crs'`, `su - grid -c '/sw/grid/bin/olsnodes -n'`, `su - grid -c '/sw/grid/bin/cluvfy comp ssa -t software -s /sw/grid/crs/install -n prmsdb1,prmsdb2 -display_status'`를 다시 실행해 `CLSD/ADR initialization failed`와 `DIA-49802`가 사라지는지 확인한다. ADRCI는 `su - grid -c 'adrci exec="show homes"'` 형식으로 실행한다.

- [2026-05-08 14:58:00] 추가 업데이트
  - 주제: ADRCI 명령 형식 및 ADR home 목록 확인
  - 추가 로그: `su - grid -c 'adrci exec="show homes"'` 실행 결과 `diag/crs/prmsdb1/crs`를 포함한 ADR home 목록이 정상 출력됨.
  - 해석: ADRCI 자체 명령 형식은 정상이며 CRS ADR home은 목록에 보인다. 다만 이것은 `/log/grid/diag/crs/prmsdb1/crs/log` leaf 디렉터리의 쓰기/실행 권한 문제가 해결됐다는 최종 증명은 아니다.
  - 다음 검증: 같은 `grid` 계정으로 `crsctl check crs`, `olsnodes -n`, `cluvfy comp ssa -t software -s /sw/grid/crs/install -n prmsdb1,prmsdb2 -display_status`를 재실행해 `DIA-49802`와 `CLSD/ADR initialization failed`가 사라졌는지 확인한다.

- [2026-05-08 15:05:00] 추가 업데이트
  - 주제: Patch `38629535` README 명령 구분 확인
  - 추가 로그: 로컬 README `C:\Users\cloudmedia\Downloads\p38629535_190000_Linux-x86-64\38629535\README.html` 기준, Grid home과 같은 버전 RAC DB homes를 함께 패치할 때는 `opatchauto apply <UNZIPPED_PATCH_LOCATION>/38629535`, Grid home만 패치할 때는 `opatchauto apply <UNZIPPED_PATCH_LOCATION>/38629535 -oh <GI_HOME>`를 사용한다.
  - 해석: README의 `software only Grid home installation or before the Grid home is configured` 케이스는 CRS가 아직 구성되지 않았거나 software-only home일 때의 분기다. 현재 로그는 `CRS-4638`, `CRS-4537`, `CRS-4529`, `CRS-4533`가 online이므로 해당 케이스로 우회할 상황이 아니라, ADR 권한 오류를 먼저 해결해야 한다.
  - 권장 조치: `/log/grid/diag/crs/prmsdb1/crs/log` 권한 수정 후 `cluvfy comp ssa`가 정상화되면, 실제 목적에 따라 전체 GI+RAC DB home 패치인지 `-oh /sw/grid` Grid-only 패치인지 선택해 재실행한다.

- [2026-05-08 15:08:00] 추가 업데이트
  - 주제: 패치 목적 확인 - Grid home과 DB home을 OPatchAuto로 함께 패치
  - 사용자 확인: Grid와 DB를 한 번에 패치하려는 목적임.
  - 권장 명령: `-oh`를 붙이지 않고 root에서 `<GI_HOME>/OPatch/opatchauto apply <UNZIPPED_PATCH_LOCATION>/38629535` 형식으로 실행한다. 예: `/sw/grid/OPatch/opatchauto apply /back/38629535`.
  - 주의: `-oh /sw/grid`는 Grid home만 패치하는 선택 명령이고, `-oh <RAC_DB_HOME>`는 특정 DB home만 패치하는 선택 명령이다. 전체 GI+RAC DB home 동시 패치 목적이면 `-oh` 없이 실행해야 한다.
