# Oracle Incident Log - errors


- [2026-08-13 09:43:29] Oracle Incident - errors
  - 질문 맥락: TAC_ConnectionPool.exe 실행 시 ODPConfig TypeInitializationException 및 OraOps19.dll DllNotFoundException 0x8007007E 발생
  - 에러 메시지: System.DllNotFoundException: DLL 'OraOps19.dll'을 로드할 수 없습니다. 지정된 모듈을 찾을 수 없습니다. HRESULT 0x8007007E
  - 원인 가설: Oracle.DataAccess.dll 4.122.19.1은 EXE 폴더에 있으나 ODP.NET Unmanaged Driver 네이티브 계층인 OraOps19.dll과 OCI 경로가 실행 프로세스의 DLL 검색 경로에 없다. 번들 ODAC에는 odp.net4\\bin\\OraOps19.dll과 instantclient\\oci.dll이 있으나 HKLM Oracle 및 ODP.NET 4.122.19.1 등록과 Oracle PATH가 없다. OS와 EXE는 64비트이며 VC++ 14 런타임은 설치되어 있다.
  - 권장 조치: 우선 현재 PowerShell 세션의 PATH 앞에 번들 ODAC의 odp.net4\\bin과 instantclient를 추가한 뒤 where.exe로 OraOps19.dll과 oci.dll이 검색되는지 확인하고 같은 창에서 EXE를 재실행한다. 성공 후에는 관리자 권한으로 번들 install.bat odp.net4 설치를 수행해 영구 배포한다. OraOps19.dll 한 파일만 단독 복사하지 않는다.
  - 검증 항목: 재실행 시 TypeInitializationException과 OraOps19.dll 오류가 사라지고 다음 단계인 Oracle 접속 성공 또는 구체적인 ORA 오류가 출력되는지 확인한다. 설치 후 HKLM ODP.NET 4.122.19.1 DllPath와 설치 경로의 bin\\OraOps19.dll 및 oci.dll을 확인한다.
- [2026-08-13 09:55:11] Oracle Incident - errors
  - 질문 맥락: mkdir C:\\Oracle\\ODAC1928 실행 시 DirectoryExist 발생
  - 에러 메시지: 지정한 이름 C:\\Oracle\\ODAC1928의 항목이 이미 있습니다. FullyQualifiedErrorId DirectoryExist
  - 원인 가설: 설치 대상 디렉터리가 이전 단계에서 이미 생성되었으며 PowerShell mkdir(New-Item)이 기존 폴더 때문에 경고한 것이다. ODAC 설치 실패를 뜻하지 않는다.
  - 권장 조치: 폴더 내용이 비어 있거나 의도한 설치 대상인지 확인하고 삭제하지 않은 채 install.bat 명령으로 진행한다.
  - 검증 항목: C:\\Oracle\\ODAC1928 내용과 install.log 존재 여부를 확인한다.
- [2026-08-13 09:58:14] Oracle Incident - errors
  - 질문 맥락: ODAC install.bat 실행 시 configure.bat, instantclient, odp.net4 등 모든 상대경로 파일을 찾지 못함
  - 에러 메시지: 파일을 찾을 수 없습니다-configure.bat; instantclient; odp.net4
  - 원인 가설: ODAC Xcopy install.bat은 원본 구성요소를 현재 작업 디렉터리의 상대경로로 찾는다. 원본 경로에 공백과 앰퍼샌드가 포함되어 PowerShell에서 배치 실행 시 작업경로 처리가 깨졌고, 설치 대상에는 install.log만 생성됐다.
  - 권장 조치: ODAC 원본 전체를 C:\\Oracle\\ODAC1928_SRC 같은 영문 및 공백 없는 경로로 복사하고, 관리자 PowerShell에서 그 폴더로 이동한 뒤 install.bat를 다시 실행한다. 설치 대상 C:\\Oracle\\ODAC1928은 유지한다.
  - 검증 항목: 재실행 출력에 실제 복사 경로와 0보다 큰 복사 파일 수가 나타나는지, 대상의 oci.dll과 bin\\OraOps19.dll이 생성되는지 확인한다.
- [2026-08-13 09:59:08] Oracle Incident - errors
  - 질문 맥락: 추가 업데이트: ODAC install.bat 콘솔의 파일 찾기 오류 이후 실제 설치 상태 재검증
  - 에러 메시지: 콘솔에는 일부 파일 찾기 오류가 표시됐으나 대상 파일과 등록 상태는 정상임
  - 원인 가설: 이전의 전체 설치 실패 판단을 정정한다. C:\\Oracle\\ODAC1928에 oci.dll 19.28, bin\\OraOps19.dll 2.122.19.1, Oracle.DataAccess.dll 4.122.19.1이 존재하며 GAC/machine.config 구성과 HKLM ODP.NET DllPath 및 KEY_ODAC1928 등록이 완료됐다.
  - 권장 조치: 설치 명령을 반복하거나 원본을 재복사하지 않는다. 새 콘솔에서 TAC_ConnectionPool.exe를 다시 실행해 다음 단계 결과를 확인한다.
  - 검증 항목: OraOps19.dll DllNotFoundException이 사라지고 접속 성공 출력 또는 구체적인 ORA 오류로 진행하는지 확인한다.
- [2026-08-13 10:03:50] Oracle Incident - errors
  - 질문 맥락: TAC_ConnectionPool.exe 실행 후 반복적으로 ORA-12541 TNS:no listener 발생
  - 에러 메시지: ORA-12541: TNS:no listener
  - 원인 가설: ODAC 네이티브 드라이버 로딩은 해결됐고 프로그램이 config의 192.168.40.191:1521로 실제 TCP 접속을 시도했으나 해당 주소와 포트에서 Oracle Listener 응답을 받지 못한다. 가능한 원인은 HOST/PORT 오입력, Listener 미기동, Listener가 다른 IP 또는 포트에서 대기, 방화벽/네트워크 차단이다.
  - 권장 조치: 클라이언트에서 Test-NetConnection 192.168.40.191 -Port 1521로 TCP 도달성을 먼저 확인한다. 실패하면 서버 측 lsnrctl status와 ss/netstat로 실제 Listener 주소 및 포트를 확인한다. TCP 성공 후에만 SERVICE_NAME과 계정을 점검한다.
  - 검증 항목: TcpTestSucceeded 여부를 확인하고, 서버의 Listener endpoint가 config HOST/PORT와 일치하는지 검증한다.
- [2026-08-13 10:06:37] Oracle Incident - errors
  - 질문 맥락: remote-session-gui로 NDBHOME1의 ORA-12541 원인 직접 검증
  - 에러 메시지: config는 192.168.40.191:1521이나 실제 LISTENER는 192.168.40.191 및 VIP 192.168.40.193의 11521에서 대기. SCAN은 192.168.40.195:11521.
  - 원인 가설: ORA-12541의 직접 원인은 잘못된 포트 1521이다. TAC 연결에는 개별 노드 IP보다 SCAN 192.168.40.195와 SCAN Listener 포트 11521을 사용하는 것이 적절하다. dmhome은 리스너에 READY이나 srvctl 서비스 리소스가 없어 기본 DB 서비스로 보이며 TAC용 별도 서비스 여부는 후속 확인이 필요하다.
  - 권장 조치: TAC_ConnectionPool.exe.config의 HOST를 192.168.40.195, PORT를 11521로 변경하고 SERVICE_NAME=dmhome은 우선 유지한 채 재실행한다.
  - 검증 항목: Windows에서 192.168.40.191:11521 및 192.168.40.195:11521 TCP 연결이 모두 성공했다. 재실행 시 ORA-12541이 사라지고 인스턴스명 또는 다음 구체적 ORA 오류가 출력되는지 확인한다.
- [2026-08-13 12:03:30] Oracle Incident - errors
  - 질문 맥락: TAF용 tnsnames.ora를 양 노드에 추가한 뒤 TAC_EXPLICIT_TX 장애 테스트에서 17번째 INSERT 시 ORA-03113 발생하고 프로그램 종료
  - 에러 메시지: ORA-03113 end-of-file on communication channel; Process ID 965424; Session ID 630 Serial 10655; ExecuteNonQuery Program.cs line 37
  - 원인 가설: 서버 tnsnames.ora의 DMHOME 별칭은 TYPE=SELECT TAF라 SELECT 커서만 복구하며 INSERT 트랜잭션을 복구하지 않는다. Windows EXE는 서버 tnsnames.ora가 아니라 exe.config의 인라인 SCAN descriptor를 사용하므로 해당 FAILOVER_MODE가 적용되지 않는다. 접속 서비스 DMHOME은 FAILOVER_TYPE NONE, AQ_HA_NOTIFICATIONS NO, COMMIT_OUTCOME NO이며 DB 세션도 FAILOVER_TYPE NONE으로 확인됐다. EXE는 ORA-03113을 catch한 뒤 종료하고 재연결 로직과 실제 Commit 호출이 없다.
  - 권장 조치: 현재 TAC_EXPLICIT_TX.exe로 셧다운 반복 테스트를 중단한다. 먼저 테스트 목표를 TAF SELECT 또는 TAC 트랜잭션으로 분리한다. TAF SELECT라면 클라이언트가 사용하는 descriptor에 FAILOVER_MODE TYPE=SELECT를 넣고 장시간 SELECT fetch 프로그램으로 검증한다. TAC라면 별도 Clusterware 서비스에 FAILOVER_TYPE=AUTO, COMMIT_OUTCOME=TRUE, AQ_HA_NOTIFICATIONS=TRUE 등을 구성하고 Commit이 포함된 올바른 테스트 프로그램을 다시 빌드한다.
  - 검증 항목: 양 노드 tnsnames.ora 내용은 동일하나 EXE config는 인라인 Data Source다. DBA_SERVICES DMHOME은 CLB_GOAL LONG 외 failover 속성이 없고 COMMIT_OUTCOME/AQ notification이 NO다. 실행 종료 후 TAC_EXPLICIT_TX 세션은 없으며 SYSTEM.TAC_TEST에는 기존 포함 94행이 있어 이번 실행 exactly-once 성공 증거가 아니다.
- [2026-08-13 14:21:38] Oracle Incident - errors
  - 질문 맥락: 대형 TAF_TEST 생성 시 데이터파일이 약 30GB까지 증가했고 테이블을 다시 만든 뒤 DATAFILE RESIZE가 되지 않음
  - 에러 메시지: 정확한 ORA 코드는 미제공. 예상되는 대표 오류는 ORA-03297: file contains used data beyond requested RESIZE value
  - 원인 가설: 삭제한 TAF_TEST가 recycle bin의 BIN$ 세그먼트로 남아 있거나 새 TAF_TEST 또는 다른 세그먼트 extent가 데이터파일 끝부분 HWM 위에 존재하여 축소 구간이 사용 중일 가능성
  - 권장 조치: 먼저 SHOW RECYCLEBIN과 USER_RECYCLEBIN에서 TAF_TEST 잔존 객체를 확인하고, 데이터파일별 현재 크기와 DBA_EXTENTS 최대 블록으로 최소 축소 가능 크기를 계산한 후 대상 세그먼트만 purge 또는 move하고 resize
  - 검증 항목: purge 또는 move 후 HWM을 다시 계산하고 요청 resize가 HWM보다 큰지 확인. DBA_DATA_FILES에서 최종 크기와 AUTOEXTENSIBLE/MAXBYTES도 확인.
- [2026-08-13 14:22:35] Oracle Incident - errors
  - 질문 맥락: Recycle Bin에 TAF_TEST 잔존 객체가 하나도 없는데 30GB 데이터파일 RESIZE가 되지 않는 경우의 다음 확인 절차
  - 에러 메시지: RESIZE 실패. 정확한 ORA 코드와 대상 데이터파일명은 아직 미제공
  - 원인 가설: 현재 TAF_TEST 또는 다른 세그먼트의 extent가 데이터파일 후미에 있어 HWM 아래로 줄이려는 것으로 추정. USER_RECYCLEBIN만 확인했다면 다른 소유자의 DBA_RECYCLEBIN 객체 가능성도 있음
  - 권장 조치: DBA_RECYCLEBIN 전체 확인 후 DBA_EXTENTS의 파일별 마지막 사용 블록으로 최소 축소 가능 크기를 계산하고, 대상 파일 후미의 세그먼트를 식별한다.
  - 검증 항목: 현재 DATAFILE 크기, HWM 기반 최소 크기, 후미 세그먼트 이름과 타입을 조회한 뒤 HWM 이상으로 RESIZE하거나 해당 세그먼트를 이동한다.
- [2026-08-13 14:46:24] Oracle Incident - errors
  - 질문 맥락: /home/oracle/taf_test/select_taf.sh 실행 중 RAC 인스턴스 종료 시 TAF SELECT가 이어지지 않고 ORA-25401 발생
  - 에러 메시지: ORA-25401: can not continue fetches. 420075 rows selected.
  - 원인 가설: 스크립트가 SELECT 결과 열에 V$INSTANCE.INSTANCE_NAME을 포함하여 장애 전 DMHOME1과 재실행 후 DMHOME2가 달라지고, ORDER BY도 없어 TAF가 이미 fetch한 행과 재실행한 행을 동일하게 검증할 수 없음. 실제 실행 세션은 SYSDBA/SYS$USERS이며 점검 당시 V$SESSION FAILOVER_TYPE/METHOD도 NONE/NONE이었음.
  - 권장 조치: 장기 조회 SQL에서는 INSTANCE_NAME을 제거하고 ID 기반 ORDER BY를 지정한다. 인스턴스명은 조회 전후 별도 SQL로 확인한다. SYSDBA가 아닌 일반 시험 계정과 TAF 전용 서비스로 접속하고 장애 전에 V$SESSION이 SELECT/BASIC/NO인지 확인한다.
  - 검증 항목: 장애 중 ORA-25401 없이 동일 SELECT fetch가 계속되고, 완료 후 인스턴스가 변경되며 FAILED_OVER=YES인지 확인한다.
- [2026-08-13 14:53:58] Oracle Incident - errors
  - 질문 맥락: SELECT instance_name, object_name FROM taf_test, v$instance 실행 중 failover 후 DMHOME2가 출력되다가 ORA-25401이 발생한 이유
  - 에러 메시지: ORA-25401: can not continue fetches. 550680 rows selected.
  - 원인 가설: 진행 중 SELECT 결과에 인스턴스별로 달라지는 V$INSTANCE.INSTANCE_NAME이 포함되어 장애 전 DMHOME1 행과 재실행 후 DMHOME2 행이 일치하지 않으므로 TAF SELECT의 prefix 검증이 실패함
  - 권장 조치: INSTANCE_NAME은 장기 SELECT 전후 별도 조회하고, 진행 중 커서는 SELECT id,object_name FROM taf_test ORDER BY id처럼 노드에 무관하고 순서가 고정된 결과만 사용한다.
  - 검증 항목: 수정된 SELECT에서 장애 후 ORA-25401 없이 fetch가 계속되고 이후 별도 쿼리에서 DMHOME2 및 FAILED_OVER=YES를 확인한다.
- [2026-08-13 15:42:13] Oracle Incident - errors
  - 질문 맥락: TAC_EXPLICIT_TX.exe가 row 8 실행 중 DMHOME1 shutdown 후 ORA-03113과 ORA-24550으로 종료됨
  - 에러 메시지: ORA-03113: end-of-file on communication channel at ExecuteNonQuery; 이후 ORA-24550 unhandled exception, kpueONSCallback 스택
  - 원인 가설: 클라이언트는 Application Continuity 옵션과 ONS 구독을 활성화했으나 접속 서비스 DMHOME은 FAILOVER_TYPE 없음, COMMIT_OUTCOME NO, AQ_HA_NOTIFICATIONS NO이고 Clusterware 별도 서비스도 없어 TAC replay가 서버에서 활성화되지 않음. ORA-24550은 ONS 콜백/풀 정리 중 발생한 2차 네이티브 클라이언트 오류.
  - 권장 조치: DMHOME2를 복구 대상으로 유지하고 DMHOME_TAC 전용 Clusterware 서비스를 FAILOVER_TYPE=AUTO, FAILOVER_RESTORE=AUTO, COMMIT_OUTCOME=TRUE, NOTIFICATION=TRUE 등으로 구성한 뒤 클라이언트 SERVICE_NAME을 변경하여 재시험. ORA-24550이 TAC 서비스 구성 후에도 반복되면 전체 클라이언트 trace와 dump로 Oracle Support 검토.
  - 검증 항목: 최신 trace에서 row 1~7 ExecuteNonQuery RetCode=0, row 8 RetCode=-1 및 ORA-03113 확인. 서버는 DMHOME1 DOWN/DMHOME2 OPEN, successful/rejected AC replays 모두 0. SYSTEM.TAC_TEST 실제 결과는 id 1~7 각 1건, 총 7건이며 id 8은 반영되지 않음.
- [2026-08-13 16:26:22] Oracle Incident - errors
  - 질문 맥락: DMHOME_TAC 접속 상태에서 DMHOME2 shutdown abort 후 TAC_EXPLICIT_TX가 ORA-03113으로 종료된 원인 확인
  - 에러 메시지: ORA-03113 at ExecuteNonQuery; latest ODP.NET trace PID 11d98 shows source dmhome2 SID 144 SERIAL 27691, failOverType=32, failover callbacks, then RetCode=-1
  - 원인 가설: 직전 장애로 DMHOME1이 내려간 뒤 프로그램 풀이 전부 DMHOME2에 접속했으며, DMHOME2까지 shutdown abort하면 살아 있는 Replay 대상 인스턴스가 없다. 또한 접속 descriptor에 RETRY_COUNT/RETRY_DELAY가 없어 서비스 재배치 동안 재접속 시도가 즉시 종료될 수 있다.
  - 권장 조치: 먼저 srvctl status database/service로 양 인스턴스 및 DMHOME_TAC 위치를 확인하고 DMHOME1을 복구한다. 두 EXE descriptor에 CONNECT_TIMEOUT, TRANSPORT_CONNECT_TIMEOUT, RETRY_COUNT=30, RETRY_DELAY=3을 추가한 후 정확히 접속 중인 한 인스턴스만 종료해 재시험한다.
  - 검증 항목: 장애 직전 두 RAC 인스턴스 RUNNING, 반대 노드가 Replay 목적지로 사용 가능, trace에서 새 인스턴스 세션 생성, 프로그램 20번까지 무예외, successful replays 증가, tac_test 중복 없음
- [2026-08-13 16:37:24] Oracle Incident - errors
  - 질문 맥락: 사용자 요청에 따라 실제 TAC_EXPLICIT_TX 장애 테스트를 직접 수행하고 TAC 작동 여부 판정
  - 에러 메시지: 양 인스턴스 OPEN 상태에서 DMHOME1 SID 142 SERIAL 15686으로 DMHOME_TAC/AUTO 접속 후 DMHOME1 shutdown abort. DMHOME2와 서비스는 생존 및 이동했지만 16번째 ExecuteNonQuery에서 ORA-03113 반환.
  - 원인 가설: RAC 인스턴스와 서비스 failover는 정상이나 현재 ODP.NET EXE의 호출 replay가 수행되지 않는다. 연결 descriptor에 RETRY_COUNT/RETRY_DELAY가 없고, 프로그램은 명시적 트랜잭션/Commit 없이 연결을 20개 INSERT 전체 동안 보유한다. 정확한 replay 미수행 원인은 다음 단계에서 연결 재시도 옵션 및 프로그램 요청 경계를 분리 검증해야 한다.
  - 권장 조치: 실패 판정 후 DMHOME1 재기동. 양 인스턴스 RUNNING, DMHOME_TAC은 DMHOME2에서 RUNNING으로 복구 확인. 현재 결과를 성공으로 간주하지 말고 클라이언트 재접속 옵션과 테스트 프로그램 구조를 수정한 별도 재시험 필요.
  - 검증 항목: 실행 전: DMHOME1/2 OPEN, DMHOME_TAC on DMHOME1. 장애 후: DMHOME2 OPEN, service relocated to DMHOME2, client ORA-03113 and stopped at row16. 복구 후 두 인스턴스 RUNNING. successful/rejected replay stats 0. 테이블 건수는 관찰 중 44에서 10으로 별도 변경되어 데이터 건수 판정은 무효.
