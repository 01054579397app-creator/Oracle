- [2026-08-14 09:13:00] Oracle Incident - errors
  - 질문 맥락: DBHOME1/2와 NDBHOME1/2에 원격 접속하여 ACTIVE Physical Standby의 ADG 전송 및 적용이 정상 진행되지 않는 원인 진단
  - 에러 메시지: Primary DMHOME2의 GV$ARCHIVE_DEST_STATUS DEST_ID=2가 ERROR이며 ORA-12514: TNS:listener does not currently know of service requested in connect descriptor. Primary SWITCHOVER_STATUS=RESOLVABLE GAP. ACTIVE MRP0는 WAIT_FOR_LOG THREAD 2 SEQUENCE 26.
  - 원인 가설: Primary 양 노드의 active TNS 별칭은 192.168.40.191:11521, SERVICE_NAME=ACTIVE를 사용하지만 NDBHOME1의 11521 리스너에 ACTIVE 서비스가 등록되어 있지 않다. ACTIVE의 SERVICE_NAMES=ACTIVE이나 LOCAL_LISTENER가 공백이고, 호스트에는 Oracle listener가 11521에만 LISTEN 중이다. 기존 1521 연결이 남은 thread 1만 일부 전송되고 새 연결이 필요한 thread 2는 ORA-12514로 중단된 비대칭 상태다.
  - 권장 조치: ACTIVE의 LOCAL_LISTENER를 NDBHOME1의 11521 listener address 또는 해당 TNS 별칭으로 지정하고 ALTER SYSTEM REGISTER를 수행한다. listener services에서 ACTIVE/ACTIVE가 READY로 등록된 뒤 Primary DEST_ID=2를 재연결하고 thread 2 sequence 26부터 gap 전송 및 적용을 확인한다. ACTIVE는 Clusterware 등록 목록에 없으므로 운영 방식에 따라 CRS 관리 대상으로 등록할지 별도로 결정한다.
  - 검증 항목: NDBHOME1 lsnrctl services에 SERVICE_NAME ACTIVE 및 instance ACTIVE READY, Primary 두 인스턴스의 GV$ARCHIVE_DEST_STATUS DEST_ID=2 VALID/ERROR 공백, ACTIVE MRP0 APPLYING_LOG 또는 WAIT_FOR_LOG가 Primary current sequence를 추종, thread 1/2 received/applied sequence 증가, transport/apply lag 감소. 진단 중 원격 설정 변경이나 재기동은 수행하지 않음.

- [2026-08-14 13:15:00] Oracle Incident - errors
  - 질문 맥락: DMHOME RAC에서 archived log backup piece 생성 후 Control File and SPFILE Autobackup 단계가 실패한 원인 확인
  - 에러 메시지: RMAN-03009 및 ORA-00245: control file backup failed; in Oracle RAC, target might not be on shared storage
  - 원인 가설: RMAN의 snapshot controlfile 경로가 `CONFIGURE SNAPSHOT CONTROLFILE NAME TO '/sw/oracle/dbs/snapcf_DMHOME1.f'`로 설정되어 있다. `/sw`는 각 노드의 로컬 XFS(`/dev/mapper/vg01-lvsw`)이므로 모든 RAC 인스턴스가 같은 파일을 공유할 수 없다. archived log backup 대상 `/back`은 양 노드에 동일한 ACFS(`/dev/asm/back_acfs-398`)로 마운트되고 oracle 쓰기도 가능하므로 `/back/archive_179.bkp`는 정상 완료됐다.
  - 권장 조치: snapshot controlfile을 공유 ACFS 경로로 변경한다. 예: `CONFIGURE SNAPSHOT CONTROLFILE NAME TO '/back/snapcf_DMHOME.f';`. 복구 시 어느 노드에서도 autobackup piece를 찾을 수 있게 controlfile autobackup format도 `CONFIGURE CONTROLFILE AUTOBACKUP FORMAT FOR DEVICE TYPE DISK TO '/back/%F';`로 명시하는 것을 권장한다.
  - 검증 항목: RMAN `SHOW SNAPSHOT CONTROLFILE NAME`과 `SHOW ALL`에서 `/back` 경로 확인, 양 노드에서 동일 파일 접근 확인, controlfile backup/autobackup 재시험 성공, `LIST BACKUP OF CONTROLFILE` 및 `/back` backup piece 확인. 진단 중 RMAN 설정 변경이나 백업 재실행은 수행하지 않음.

  - 추가 업데이트(13:16): DBHOME1과 DBHOME2에 각각 직접 RMAN 접속하여 재확인했다. DMHOME1은 기본 snapshot controlfile `/sw/oracle/dbs/snapcf_DMHOME1.f`, DMHOME2는 `/sw/oracle/dbs/snapcf_DMHOME2.f`를 표시했다. 양 노드의 `/sw`는 각자의 `/dev/mapper/vg01-lvsw` XFS이고, `/back`은 동일한 `/dev/asm/back_acfs-398` ACFS이며 oracle 쓰기 가능하다. 노드별 로컬 snapshot controlfile 기본값이 ORA-00245의 직접 원인임을 확정했다.
  - 추가 업데이트(13:18): 양 인스턴스 alert log의 13:12:36 메시지를 확인했다. DMHOME2 CKPT가 DMHOME1이 생성 중인 `/sw/oracle/dbs/snapcf_DMHOME1.f`를 열지 못해 `ORA-27037: unable to obtain file status`, `Linux Error: 2: No such file or directory`를 기록했다. 이어 DMHOME1은 incomplete backup control file을 제거하고 `ORA-00245`를 반환했다. 즉 다른 RAC 인스턴스가 노드 1 로컬 snapshot controlfile에 접근할 수 없었던 것이 실제 선행 오류다.

- [2026-08-14 13:22:00] Oracle Incident - errors
  - 질문 맥락: RMAN compressed archived-log backup piece `/back/archive_179.bkp`를 ACTIVE standby에 `ALTER DATABASE REGISTER LOGFILE`로 등록하려다 실패했고 압축 archived log 사용 가능 여부 확인
  - 에러 메시지: ORA-01900: LOGFILE keyword expected, 이어 ORA-00317: file type 0 in header is not log file 및 ORA-00334. ACTIVE에서 `LIST BACKUPPIECE` 조회 시 RMAN-20260로 아직 repository에 미등록 상태 확인.
  - 원인 가설: `archive_179.bkp`는 archived log 원본 파일이 아니라 thread 1 sequence 41~44와 thread 2 sequence 41~43을 담은 RMAN compressed backup set piece다. SQL REGISTER LOGFILE은 archived redo log 헤더를 기대하므로 backup piece 헤더를 거부한다.
  - 권장 조치: ACTIVE RMAN에서 `CATALOG BACKUPPIECE '/back/archive_179.bkp';`로 piece를 등록한 뒤 `RESTORE ARCHIVELOG`로 원본 archived log를 `/arch/ACTIVE`에 복원한다. thread 1은 FROM SEQUENCE 41 UNTIL SEQUENCE 45, thread 2는 FROM SEQUENCE 41 UNTIL SEQUENCE 44를 사용한다. UNTIL SEQUENCE는 상한 미포함이다.
  - 검증 항목: catalog 후 LIST BACKUPPIECE 성공, 복원된 thread/sequence 확인, V$ARCHIVED_LOG 등록 확인, MRP 적용 sequence 증가. 조회 당시 ACTIVE는 DBID 1454048457, PHYSICAL STANDBY, MOUNTED이고 piece 파일은 3.7MB로 존재함. 실제 catalog/restore는 수행하지 않음.
