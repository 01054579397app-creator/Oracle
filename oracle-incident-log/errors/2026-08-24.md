# Oracle Incident Log - errors

- [2026-08-24 08:49:00] Oracle Incident - errors
  - 질문 맥락: 192.168.40.172/173 PRIMARY RAC와 191 ACTIVE, 192 FSYNC에서 사용자 DML이 없는데도 아카이브 로그가 계속 생성·누적되는 원인을 실서버 기준으로 확인. 사용자가 PRIMARY에서 `RMAN> delete archivelog all;`을 실행한 결과도 함께 검증함.
  - 에러 메시지: FSYNC와 ACTIVE에서 `ORA-19504: failed to create file` → `ORA-27044: unable to write the header block of file` → `Linux Error: 28: No space left on device` → `ORA-16038`; PRIMARY에서 FSYNC 목적지 `ORA-16086`, ACTIVE FAL 전송 `ORA-00270`/`ORA-16055`; ACTIVE MRP에서 `ORA-19906` → `ORA-19909: datafile 1 belongs to an orphan incarnation`.
  - 원인 가설: 확인 결과 실제 원인은 사용자 INSERT가 아니라 Data Guard 장애 사슬임. 191/192가 공유하는 9.9GB `/arch`가 가득 차 ACTIVE/FSYNC가 SRL을 아카이브하지 못했고, PRIMARY는 실패한 FSYNC 목적지를 `REOPEN_SECS=300`으로 재시도할 때마다 `ORA-16086`과 함께 작은 redo를 약 5분 간격으로 로그 스위치함. ACTIVE에서는 08:40 MRP가 incarnation 불일치로 종료되어 수신 로그를 적용하지 못하고 있으며, 공간이 확보된 뒤에는 과거 gap 로그가 RFS로 한꺼번에 재전송되어 파일 수가 급증함.
  - 권장 조치: PRIMARY 아카이브를 추가로 강제 삭제하지 말고 ACTIVE의 incarnation/datafile 불일치를 먼저 복구하거나 standby를 재구성한 뒤 MRP를 정상 재개함. 이후 FSYNC의 정체된 SRL과 PRIMARY의 `ORA-16086` 해소를 확인하고, 9.9GB 공유 `/arch`의 보존·백업·삭제 정책 및 용량을 재설계함.
  - 검증 항목: PRIMARY 온라인 redo는 thread별 200MB이나 최근 아카이브는 약 0.25~4MB로 미사용 상태에서 약 5분마다 스위치됨. `ARCHIVE_LAG_TARGET=0`, FSYNC 목적지 `REOPEN_SECS=300`. 08:49 기준 PRIMARY 잔존 파일은 thread 1 sequence 138~259 122개, thread 2 sequence 116~240 125개(실파일 247개, 약 606.6MB). RMAN 출력은 `RMAN-08137: needed for standby`로 이 구간을 보호하고 과거 145개만 삭제함. ACTIVE 수신은 thread 1/2 각각 259/240, 적용은 138/115이며 MRP 프로세스가 없음. 공유 `/arch`는 9.9GB 중 4.8GB 사용, `/arch/ACTIVE`가 4.3GB임.

- [2026-08-24 08:58:00] 추가 업데이트
  - 질문 맥락: 현재 무엇이 문제여서 Data Guard가 정상 동작하지 않는지 재확인.
  - 에러 메시지: ACTIVE에서 MRP 기동 시마다 `ORA-19909: datafile 1 belongs to an orphan incarnation`과 `ORA-01110`이 발생하며 automatic flashback 실패 후 MRP0가 종료됨. PRIMARY의 FSYNC 목적지는 계속 `ORA-16086` 상태임.
  - 원인 가설: 현재 최우선 차단점은 네트워크나 redo 수신이 아니라 ACTIVE controlfile incarnation과 datafile header의 불일치임. 2026-08-20 11:23 ACTIVE에서 `ALTER DATABASE ACTIVATE STANDBY DATABASE`가 실행되어 11:24 RESETLOGS incarnation 2가 생성됨. 현재 PRIMARY와 ACTIVE의 모든 datafile header는 원래 incarnation 1(`RESETLOGS_CHANGE#=1`)인데 ACTIVE controlfile의 `V$DATABASE_INCARNATION`은 incarnation 2(`RESETLOGS_CHANGE#=3708336`)를 CURRENT로 가리켜, MRP가 모든 datafile을 orphan branch로 판정함.
  - 권장 조치: 반복적인 MRP 재시작이나 추가 아카이브 삭제로는 해결되지 않음. PRIMARY incarnation 1과 일치하는 standby controlfile/datafile 복구 경로를 검증해야 하며, ACTIVE가 실제로 activation 및 RESETLOGS를 거친 이력이 있으므로 운영 안전성 기준으로는 PRIMARY에서 ACTIVE standby를 refresh/rebuild하는 방안을 우선 검토함. 그 후 MRP apply 정상화와 FSYNC의 정체된 SRL/`ORA-16086`을 순서대로 복구함.
  - 검증 항목: 08:57 기준 PRIMARY→ACTIVE `transport lag=0`으로 최신 thread 1/2 sequence 265/247까지 수신했지만 적용은 138/115에서 정지했고 `apply lag=11:03:02`, MRP 프로세스 없음. PRIMARY는 incarnation 1 CURRENT, ACTIVE controlfile은 incarnation 1 PARENT 및 incarnation 2 CURRENT, 54개 datafile header는 모두 incarnation 1. FSYNC 내부 목적지는 VALID로 회복 중이나 PRIMARY에서는 FSYNC가 ALTERNATE/`ORA-16086`이며 오래된 SRL sequence 117/118 및 94/95가 남아 있음.

- [2026-08-24 09:01:00] 정정 업데이트
  - 질문 맥락: FSYNC 구성 후 ADG가 정상 동작했으므로 8/20 activation 이력을 현재 장애 원인으로 단정한 이전 분석을 재검증함.
  - 에러 메시지: MRP는 08:40 직전까지 incarnation 1 로그를 thread 1 sequence 138, thread 2 sequence 116까지 정상 적용함. 08:40:26 `Setting recovery target incarnation to 2` 직후 `ORA-19906: recovery target incarnation changed during recovery`, 이어서 재기동 시 `ORA-19909` 발생.
  - 원인 가설: 이전 분석을 정정함. 8/20 activation 자체는 현재 장애의 직접 트리거가 아님. 08:40:20 ACTIVE에 접속한 RMAN 세션에서 `/arch` 전체에 대한 CATALOG가 실행되어, 정상 branch `1240970889` 파일과 함께 과거 activation branch `1241781858`의 `1_1`~`1_6`, `2_1` 아카이브가 ACTIVE controlfile에 등록됨. 이 CATALOG 시점에 recovery target이 incarnation 2로 바뀌어 정상 실행 중이던 MRP가 중단됨. `DELETE ARCHIVELOG ALL`은 그 이후 08:40:31 시작되어 142개를 삭제함.
  - 권장 조치: 현재 직접 복구 대상은 FSYNC 구성이 아니라 ACTIVE controlfile의 recovery target을 정상 PRIMARY branch/incarnation 1로 되돌리는 것임. 변경 전 `LIST INCARNATION`, datafile header, 필요한 다음 sequence 보유 여부를 대조하고 ACTIVE에서 incarnation 1 선택 후 MRP 재기동을 검증하는 경로를 사용함. 공용 `/arch` 루트에 다시 `CATALOG START WITH`를 실행하지 말고 DB_UNIQUE_NAME 및 resetlogs branch가 확인된 파일만 대상으로 함.
  - 검증 항목: `V$RMAN_STATUS` session 70은 08:40:20~08:40:35 실행되었고 RMAN output에 `searching for all files that match the pattern /arch`, 과거 branch 파일 catalog, 이어서 DELETE 작업이 확인됨. alert의 recovery target 변경은 DELETE 시작 전 발생함. 따라서 FSYNC 구성 후 정상 ADG였다는 사용자 설명과 최신 로그가 일치하며, 현재 장애의 직접 트리거는 08:40 `/arch` 전체 CATALOG임.

- [2026-08-24 09:04:00] 추가 업데이트
  - 질문 맥락: ACTIVE의 recovery target을 정상 PRIMARY branch로 되돌리고 ADG Redo Apply를 재개하기 위해 순서대로 실행할 명령 요청.
  - 에러 메시지: 잘못 선택된 incarnation 2에서 MRP 기동 시 `ORA-19909`; 정상 MRP 중 recovery target 변경 당시 `ORA-19906`.
  - 원인 가설: ACTIVE datafile header와 PRIMARY는 `RESETLOGS_CHANGE#=1`인 원래 branch인데, `/arch` 전체 CATALOG 후 ACTIVE controlfile의 recovery target만 과거 activation incarnation 2로 변경됨.
  - 권장 조치: `.191`의 `DB_UNIQUE_NAME=ACTIVE`, `DATABASE_ROLE=PHYSICAL STANDBY`를 먼저 확인하고 MRP 정지 및 MOUNT 상태에서 RMAN `LIST INCARNATION`을 실행함. 출력이 기존 확인값인 정상 branch `Inc Key 1`, `Reset SCN 1`과 일치할 때만 `RESET DATABASE TO INCARNATION 1`을 실행함. 이후 READ ONLY로 열고 `ALTER DATABASE RECOVER MANAGED STANDBY DATABASE DISCONNECT FROM SESSION`으로 적용을 재개함. 키나 역할이 다르면 RESET을 실행하지 않음.
  - 검증 항목: `V$DATABASE_INCARNATION`에서 incarnation 1이 CURRENT로 전환됐는지, `V$DATABASE.OPEN_MODE`가 `READ ONLY WITH APPLY`인지, `MRP0`가 존재하며 applied sequence가 138/116 이후로 증가하는지, `V$ARCHIVE_GAP` 및 alert log에 새 ORA-19906/19909가 없는지 확인함.

- [2026-08-24 09:17:00] 복구 완료 업데이트
  - 질문 맥락: Codex가 실서버에서 직접 ACTIVE와 FSYNC를 모두 복구하고, 실제 입력 명령·출력·잘못된 조회 및 정정 과정을 실행 순서대로 정리해 달라는 요청.
  - 에러 메시지: ACTIVE는 잘못된 recovery target으로 `ORA-19906`/`ORA-19909`; FSYNC는 누락된 SRL ASM 멤버로 `ORA-00313` → `ORA-17503` → `ORA-15173`/`ORA-15012`, PRIMARY는 `ORA-16086`으로 FSYNC를 우회함.
  - 원인 가설: ACTIVE는 `/arch` 전체 CATALOG로 과거 branch가 선택된 것이 직접 원인. FSYNC는 controlfile의 SRL 그룹 10/11/16/17 등록은 남았지만 `+DATA_HOME/FSYNC/ONLINELOG` 실제 파일이 전부 없어 수신 SRL을 선택할 수 없었음.
  - 권장 조치: 실행 완료. ACTIVE controlfile 백업 후 RMAN `RESET DATABASE TO INCARNATION 1`, READ ONLY OPEN 및 MRP 재기동. FSYNC controlfile을 정지 상태에서 복사하고 `STANDBY_FILE_MANAGEMENT=AUTO`에서 네 SRL을 `CLEAR UNARCHIVED LOGFILE`로 OMF 재생성한 뒤 원래 `MANUAL`로 복원. PRIMARY dest 3을 DEFER/ENABLE하고 로그 스위치하여 FSYNC 우선 경로로 failback함.
  - 검증 항목: 09:16 기준 PRIMARY는 MAXIMUM AVAILABILITY, 양쪽 인스턴스 FSYNC dest 3 VALID/SYNC AFFIRM, ACTIVE 직송 dest 2 ALTERNATE. FSYNC는 current thread 1/2 sequence 276/260을 수신·전달하고 ACTIVE는 READ ONLY WITH APPLY, MRP0 APPLYING_LOG sequence 276, transport/apply lag 0, archive gap 없음. 09:15:14 이후 FSYNC Error/Fatal 없음. 전체 실제 기록은 `oracle-incident-log/execution/2026-08-24.md`에 저장함.
