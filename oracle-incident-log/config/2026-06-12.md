# Oracle Incident Log - config

- [2026-06-12 14:59:46] Oracle Incident - config
  - 질문 맥락: Oracle Grid Infrastructure 19c response file에서 사용자가 작성한 항목 중 기본값으로 볼 수 있는 값 구분 요청
  - 에러 메시지: 해당 없음
  - 원인 가설: silent install response file에는 템플릿 기본값, 설치 방식에 따른 선택값, 호스트/스토리지 환경값이 섞여 있어 불필요한 기본값 제거 여부 판단이 필요함
  - 권장 조치: 로컬 `grid_install.rsp` 템플릿과 대조해 `false`, `NONE`, `EXTERNAL`, `4`, 그룹명 등 기본값과 SCAN/노드/네트워크/ASM 디스크 같은 환경 지정값을 분리함
  - 검증 항목: response file version, GNS/GIMR/IPMI/AFD/RHPS/관리 옵션 등 템플릿 기본값 여부와 CRS_CONFIG, clusterNodes, networkInterfaceList, diskGroup disks 등 필수 환경값 여부 확인

  - 추가 업데이트 [2026-06-12 15:02:44]: 사용자가 말한 기본값의 의미를 "값이 기본값과 같음"이 아니라 "response file에서 줄 자체를 삭제해도 되는 항목"으로 정정함.
  - 추가 원인 가설: 필수 설치 토폴로지 항목과 미사용 기능을 끄는 선택 항목이 섞여 있어, 단순히 값이 기본값과 같다는 이유로 삭제하면 silent install 검증에서 필수값 누락 또는 의도치 않은 자동 root 실행이 발생할 수 있음.
  - 추가 권장 조치: GNS/GIMR/IPMI/AFD/RHPS/Cloud Control 미사용 항목, upgrade 전용 `ignoreDownNodes`, EXTERNAL redundancy에서 중복된 `disksWithFailureGroupNames`, Cloud Control 미사용 시 `omsPort`는 삭제 가능 후보로 안내함. `responseFileVersion`, `CRS_CONFIG`, `ClusterConfiguration`, SCAN, clusterNodes, networkInterfaceList, ASM disk group/disks/discovery string, manual root 실행을 위한 `executeRootScript=false`는 유지 대상으로 안내함.
  - 추가 검증 항목: 삭제 후 `gridSetup.sh -silent -responseFile <file>` 실행 전 `-executePrereqs` 또는 installer validation 로그에서 필수 response variable 누락 여부 확인.
