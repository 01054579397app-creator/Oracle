# Oracle Incident Log - config


- [2026-03-24 09:40:41] Oracle Incident - config
  - 질문 맥락: Oracle GoldenGate Core 설치 중 `/sw/ogg` 경로 비어있지 않음 경고와 클러스터에서 소프트웨어 위치 공유 여부를 판단하지 못했다는 경고가 발생했지만 설치는 성공적으로 완료됨
  - 에러 메시지: INS-75003 The specified directory /sw/ogg is not empty; INS-75017 Cluster detected: Unable to determine if the specified software location is shared; OUI-10030 non-empty directory warning; 최종 결과는 `Successfully Setup Software` 및 `The installation of Oracle GoldenGate Core was successful`
  - 원인 가설: 1) 대상 Oracle Home(`/sw/ogg`)에 기존 파일 또는 이전 설치 잔재가 남아 있음 2) 클러스터 환경에서 해당 경로가 공유 스토리지인지 OUI가 판별하지 못했거나 일부 노드에서 접근 확인이 되지 않음 3) 경고는 설치 차단 조건이 아니라 권고 수준이어서 바이너리 배치는 정상 완료됨
  - 권장 조치: 1) `/sw/ogg`가 신규 GoldenGate Home이라면 기존 파일 성격을 확인하고 필요 없는 잔재는 정리 2) RAC/클러스터라면 각 노드에서 `/sw/ogg` 접근성과 동일 경로 매핑 여부를 확인하고, shared storage 사용 정책인지 local storage per node 정책인지 명확히 결정 3) 설치 후 `ggsci`, `opatch lsinventory`, `inventory.xml` 또는 oraInventory 로그로 실제 Home 등록 상태를 검증 4) 추후 패치/업그레이드 전에는 비어 있는 새 Oracle Home 사용을 우선 고려
  - 검증 항목: `ls -la /sw/ogg`로 기존 파일 확인 / 각 노드에서 동일 경로 마운트 확인 / `$OGG_HOME/ggsci` 실행 확인 / `opatch lsinventory` 또는 oraInventory에서 GoldenGate Home 정상 등록 확인 / `/log/oraInventory/logs/installActions2026-03-24_09-37-50AM.log`와 `silentInstall2026-03-24_09-37-50AM.log`에 fatal 오류 없음 확인
