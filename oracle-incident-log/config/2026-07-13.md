# Oracle Incident Log - config


- [2026-07-13 12:53:34] Oracle Incident - config
  - 질문 맥락: AS-IS에서 TO-BE로 이관한 UTL_HTTP HTTPS 서비스의 Host ACL, Wallet ACL, OS Wallet 및 인증서가 정상 이관되었는지 확인하고 테스트 절차를 정리해 달라는 요청
  - 에러 메시지: 현재 확인된 ORA 오류 없음. 이관 후 ACL 및 Wallet 누락 가능성 점검 단계
  - 원인 가설: 스키마 단위 이관에서는 Host/Wallet ACE 같은 DB 딕셔너리 설정과 ewallet.p12/cwallet.sso 같은 DB 서버 OS 파일이 자동 승계되지 않았을 가능성이 있다. 실제 호출 스키마와 PDB, 포트, wallet_path가 AS-IS와 다를 수도 있다.
  - 권장 조치: 실제 실행 스키마와 PDB 확인; AS-IS/TO-BE DBA_HOST_ACES와 DBA_WALLET_ACES 비교; TO-BE DB 서버에서 DNS/TCP/TLS 확인; Oracle OS 계정의 wallet 파일 읽기 권한 및 orapki display 확인; UTL_HTTP detailed exception 테스트; 누락된 최소 ACE 또는 CA 체인만 반영 후 재시험
  - 검증 항목: TO-BE 실제 서비스 스키마로 호출하여 HTTP status code 수신. ORA-24247, ORA-28759, ORA-29024 없이 응답을 받고 애플리케이션 업무 호출까지 확인. 웹 인증서 교체 전후 동일 테스트 수행.
