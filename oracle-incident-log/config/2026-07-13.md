# Oracle Incident Log - config


- [2026-07-13 12:53:34] Oracle Incident - config
  - 질문 맥락: AS-IS에서 TO-BE로 이관한 UTL_HTTP HTTPS 서비스의 Host ACL, Wallet ACL, OS Wallet 및 인증서가 정상 이관되었는지 확인하고 테스트 절차를 정리해 달라는 요청
  - 에러 메시지: 현재 확인된 ORA 오류 없음. 이관 후 ACL 및 Wallet 누락 가능성 점검 단계
  - 원인 가설: 스키마 단위 이관에서는 Host/Wallet ACE 같은 DB 딕셔너리 설정과 ewallet.p12/cwallet.sso 같은 DB 서버 OS 파일이 자동 승계되지 않았을 가능성이 있다. 실제 호출 스키마와 PDB, 포트, wallet_path가 AS-IS와 다를 수도 있다.
  - 권장 조치: 실제 실행 스키마와 PDB 확인; AS-IS/TO-BE DBA_HOST_ACES와 DBA_WALLET_ACES 비교; TO-BE DB 서버에서 DNS/TCP/TLS 확인; Oracle OS 계정의 wallet 파일 읽기 권한 및 orapki display 확인; UTL_HTTP detailed exception 테스트; 누락된 최소 ACE 또는 CA 체인만 반영 후 재시험
  - 검증 항목: TO-BE 실제 서비스 스키마로 호출하여 HTTP status code 수신. ORA-24247, ORA-28759, ORA-29024 없이 응답을 받고 애플리케이션 업무 호출까지 확인. 웹 인증서 교체 전후 동일 테스트 수행.
- [2026-07-13 14:07:30] Oracle Incident - config
  - 질문 맥락: Wallet 권한과 Host 접근 권한이 DBA_SYS_PRIVS, DBA_TAB_PRIVS, DBA_ROLE_PRIVS에 나타나지 않는 이유
  - 에러 메시지: 오류 없음. 권한 저장 구조에 대한 개념 문의
  - 원인 가설: Host 및 Wallet 접근 권한은 SQL 시스템 권한, 스키마 객체 권한, 역할 부여가 아니라 외부 네트워크 대상과 Wallet 경로에 연결되는 Network ACL의 ACE이므로 일반 GRANT 딕셔너리 뷰에 저장되지 않는다.
  - 권장 조치: Host 권한은 DBA_HOST_ACES 또는 USER_HOST_ACES, Wallet 권한은 DBA_WALLET_ACES 또는 USER_WALLET_ACES에서 조회한다. ACL 대상 연결은 DBA_HOST_ACLS와 DBA_WALLET_ACLS에서 확인한다. DBMS_NETWORK_ACL_ADMIN 패키지 자체 EXECUTE 권한은 별개의 객체 권한이므로 DBA_TAB_PRIVS에서 확인한다.
  - 검증 항목: 대상 사용자 기준으로 DBA_HOST_ACES와 DBA_WALLET_ACES를 조회하여 principal, privilege, grant_type을 확인하고 실제 UTL_HTTP 호출 결과와 대조한다.
- [2026-07-13 14:08:24] Oracle Incident - config
  - 질문 맥락: DBA_SYS_PRIVS, DBA_TAB_PRIVS, DBA_ROLE_PRIVS 외에 별도 뷰나 저장소에서 관리되는 Oracle 권한과 접근통제 종류 문의
  - 에러 메시지: 오류 없음. Oracle 권한 체계 분류 문의
  - 원인 가설: Oracle에는 SQL 시스템/객체/역할 권한 외에도 Network ACL, password file 행정 권한, proxy 접속, Java policy, Database Vault, VPD/RLS, Real Application Security, TDE keystore 및 OS 파일 권한 등 별도 권한 또는 접근통제 계층이 존재한다.
  - 권장 조치: 이관 점검 시 DBA_HOST_ACES/DBA_WALLET_ACES, V$PWFILE_USERS, DBA_PROXIES, DBA_JAVA_POLICY, DBA_DV_REALM_AUTH, DBA_POLICIES, DBA_XS_ACES 및 V$ENCRYPTION_WALLET과 OS 파일 권한을 사용 기능에 따라 확인한다. 단순 애플리케이션 DB라면 Network ACL과 password file, proxy, Java policy를 우선 확인한다.
  - 검증 항목: AS-IS/TO-BE에서 사용 중인 기능별 전용 뷰 결과와 OS 파일을 비교하고 실제 관리 접속, UTL_HTTP, proxy, Java, 암호화 키 접근을 각각 기능 테스트한다.
- [2026-07-13 15:36:42] Oracle Incident - config
  - 질문 맥락: UTL_HTTP에서 Wallet 권한과 Host 접근 권한의 차이 문의
  - 에러 메시지: 오류 없음. Network ACL 개념 구분 문의
  - 원인 가설: Host ACE는 특정 DB 사용자 또는 역할이 외부 Host와 Port로 네트워크 연결할 수 있는지를 통제한다. Wallet ACE는 Wallet 안에 저장된 클라이언트 인증서 또는 사용자 비밀번호 Credential을 외부 인증에 사용할 수 있는지를 통제한다. 일반 HTTPS 서버 인증서 신뢰만 수행할 때는 Host connect ACE와 CA가 들어 있는 Wallet 및 OS 읽기 권한이 핵심이며 Wallet ACE는 보통 필요하지 않다.
  - 권장 조치: 일반 HTTPS, Wallet 저장 비밀번호 인증, mTLS 세 경우로 구분한다. 일반 HTTPS에는 Host connect와 trusted CA Wallet을, Wallet 저장 Credential 사용에는 use_passwords를, mTLS에는 use_client_certificates를 설정한다. Wallet 파일의 OS 읽기 권한은 별도로 확인한다.
  - 검증 항목: 실제 서비스의 인증방식이 단방향 HTTPS인지 mTLS인지 확인하고, DBA_HOST_ACES 및 DBA_WALLET_ACES 결과와 Wallet 인증서 종류를 대조한다. 서비스 계정으로 HTTP status와 실제 업무 인증을 각각 시험한다.
