# Oracle Incident Log - config


- [2026-08-12 14:07:14] Oracle Incident - config
  - 질문 맥락: Oracle Linux 9.7 서버에서 ISO 로컬 저장소로 필수 패키지를 설치하는데 DNS 오류가 발생함
  - 에러 메시지: file:///media AppStream/BaseOS를 구성했지만 DNF가 DNS 관련 원격 저장소 오류를 출력함
  - 원인 가설: 로컬 저장소 외에 ol9_UEKR8 원격 저장소가 enabled 상태라 DNF가 모든 활성 저장소 메타데이터를 함께 조회함
  - 권장 조치: 오프라인 설치 시 dnf --disablerepo=* --enablerepo=AppStream --enablerepo=BaseOS install ... 형식으로 로컬 저장소만 지정한다. NDBHOME2는 /dev/sr0 마운트와 local.repo 생성부터 수행한다.
  - 검증 항목: NDBHOME1에서 ISO는 /media에 정상 마운트되고 repomd.xml이 존재했다. 로컬 저장소만 지정한 --assumeno 해석은 완료되었으며 요청 패키지는 전부 이미 설치된 상태였다. NDBHOME2는 /media 미마운트 및 local.repo 미생성 상태였다.
- [2026-08-12 14:13:35] Oracle Incident - config
  - 질문 맥락: DNF 원격 저장소가 왜 활성화되고 로컬 ISO 저장소만 어떻게 지정하는지 확인
  - 에러 메시지: DNS가 없는 환경에서도 ol9_UEKR8 원격 저장소 조회가 시도됨
  - 원인 가설: oraclelinux-release-el9 패키지가 제공하는 uek-ol9.repo의 ol9_UEKR8 항목이 enabled=1로 기본 설정되어 있음
  - 권장 조치: 일회성 설치는 --disablerepo=*와 --enablerepo=AppStream/BaseOS를 사용한다. 영구 오프라인 운영 시 해당 원격 저장소들을 config-manager로 비활성화한다.
  - 검증 항목: NDBHOME1과 NDBHOME2 모두 /etc/yum.repos.d/uek-ol9.repo의 ol9_UEKR8 enabled=1 확인. NDBHOME2는 oracle-linux-ol9.repo의 ol9_baseos_latest와 ol9_appstream도 enabled=1. NDBHOME1은 해당 파일이 oracle-linux-ol9.repo.back으로 변경되어 DNF 대상에서 제외됨.
- [2026-08-12 14:24:25] Oracle Incident - config
  - 질문 맥락: Oracle RAC 준비 서버에서 multipathd는 active지만 failed to get udev uid와 multipath -ll 공백 발생
  - 에러 메시지: sda/sdb/sde failed to get udev uid, failed to get path uid, uevent trigger error; 공유 디스크는 wwid not in wwids file skipping
  - 원인 가설: 두 노드의 multipath.conf가 find_multipaths yes/on으로 설정되어 단일 경로 VMware 공유 디스크가 wwids 파일 미등록 상태에서 제외됨. 로컬 OS/LVM 디스크는 VMware WWID가 없어 경고 발생.
  - 권장 조치: 현재 구축 문서 의도대로라면 두 노드에서 defaults의 find_multipaths를 no로 변경한 뒤 multipathd 재로드 및 multipath 맵 생성 여부를 확인한다. 로컬 디스크 경고는 WWID 부재에 따른 것으로 공유 디스크 장애와 구분한다.
  - 검증 항목: 두 노드에서 공유 디스크 5개의 동일한 WWID를 확인했으며 multipath -v3에서 모두 not in wwids file, skipping으로 확인됨. 현재 multipath -ll은 비어 있음.
