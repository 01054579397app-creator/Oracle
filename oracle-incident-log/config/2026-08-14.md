- [2026-08-14 15:21:37] Oracle Incident - config
  - 질문 맥락: Oracle GoldenGate 운영 계정의 비밀번호를 주기적으로 변경할 때 DB 계정과 Credential Store를 어떤 순서로 전환해야 하는지 확인
  - 현재 구성: NDBHOME1의 OGG Classic 19.1.0.0.4 Credential Store 기본 도메인에 `src=ggadmin@oggsrc`, `trg=ggadmin@oggtrg`가 있다. `EXT1`은 `USERIDALIAS src`, `rep1.prm`은 `USERIDALIAS trg`를 사용한다. Manager에는 `AUTOSTART ER *`와 `AUTORESTART ER *, WAITMINUTES 1, RETRIES 10`이 설정되어 있다. 현재 REP1 그룹은 GGSCI에 등록되지 않았고 EXT1은 별도 trail 등록 오류로 ABENDED 상태다. `oggsrc`는 192.168.40.150:1521의 DMRMS, `oggtrg`는 192.168.40.191:11521의 DMHOME을 가리킨다.
  - 권장 순서: 소스와 타깃을 동시에 바꾸지 말고 alias별로 한쪽씩 처리한다. 무중단 우선이면 전용 DB 프로파일에 `PASSWORD_ROLLOVER_TIME`을 설정하고 DB에서 새 비밀번호로 변경하여 구/신 비밀번호 병행 기간을 시작한 다음, OGG 서버에서 `ALTER CREDENTIALSTORE REPLACE USER ggadmin@<tns_alias> ALIAS <alias>`를 PASSWORD 절 없이 실행해 숨김 프롬프트로 새 비밀번호를 저장한다. 이어 `DBLOGIN USERIDALIAS <alias>`를 검증하고 영향 프로세스의 재연결/재시작 검증 후 롤오버 기간을 종료한다. 롤오버를 사용하지 않으면 AUTORESTART를 고려해 영향 Extract/Replicat을 명시적으로 STOPPED로 통제하고 DB 비밀번호 변경, Credential Store REPLACE, DBLOGIN 검증, 프로세스 START 순서로 짧은 점검 시간에 수행한다.
  - 현재 환경 적용: 활성 Replicat이 없으므로 `trg`를 먼저 변경·DBLOGIN 검증한 뒤 `src`를 변경하는 것이 안전하다. EXT1은 현재 ABENDED이므로 trail 등록 문제를 해결해 시작하기 전에 `src` 갱신을 완료한다. 설치된 GGSCI 도움말은 `PASSWORD` 절을 쓰면 비밀번호가 화면에 평문으로 표시되므로 생략하고 숨김 프롬프트를 쓰도록 명시한다.
  - 검증 항목: 변경 전 alias 소비 프로세스와 DB 프로파일 확인, `DBLOGIN USERIDALIAS src/trg` 성공, `INFO CREDENTIALSTORE`의 alias/userid 유지, 프로세스 RUNNING, report/ggserr.log에 ORA-01017 또는 OGG 로그인 오류 없음, checkpoint/lag/trail 증가 확인. 비밀번호 자체는 로그나 명령 이력에 기록하지 않는다. 이번 확인에서는 DB 비밀번호, Credential Store, OGG 프로세스를 변경하지 않았다.
  - 추가 업데이트(15:34): 사용자 조회 결과 GGADMIN은 DEFAULT 프로파일, ACCOUNT_STATUS=OPEN, PASSWORD_ROLLOVER_TIME=0으로 확인되어 현재 점진적 롤오버가 비활성화되어 있다. DEFAULT 프로파일을 직접 변경하면 다른 사용자에게도 적용되므로 `OGG_SERVICE_PROFILE` 같은 전용 프로파일을 `PASSWORD_ROLLOVER_TIME 1/24` 이상으로 생성하고 GGADMIN에만 할당하는 방식을 권장한다. 새 프로파일에서 생략한 제한은 DEFAULT 프로파일의 값을 상속한다. 프로파일 할당 후 비밀번호를 변경해야 `OPEN & IN ROLLOVER` 상태가 시작된다. 이 출력만으로 소스 DMRMS와 타깃 DMHOME 중 어느 DB인지 식별되지 않으므로 양쪽에서 각각 조회·적용해야 하며, 이번 단계에서는 설정을 변경하지 않았다.
  - 추가 업데이트(15:35): 전용 프로파일은 영구 유지할 필요가 없다. `GGADMIN`에 임시 `OGG_ROLLOVER_PROFILE`을 할당하고 비밀번호 변경, OGG Credential Store 갱신, `DBLOGIN USERIDALIAS` 및 프로세스 재연결 검증을 완료한 뒤 `ALTER USER GGADMIN EXPIRE PASSWORD ROLLOVER PERIOD`로 구 비밀번호를 폐기한다. 이후 `ALTER USER GGADMIN PROFILE DEFAULT`로 복귀시키고 사용자가 남아 있지 않은 임시 프로파일을 `DROP PROFILE OGG_ROLLOVER_PROFILE`로 제거할 수 있다. `DEFAULT` 프로파일 자체를 일시 변경하는 것도 기술적으로 가능하지만 변경 시간 동안 같은 프로파일의 모든 계정에 적용되므로 영향 범위가 큰 방식이다.
  - 추가 업데이트(15:35:47): 운영 변경 창에서 다른 DEFAULT 계정의 비밀번호 변경을 통제할 수 있다면 DEFAULT 프로파일 자체를 일시 변경하는 방식도 유효하다. 순서는 `ALTER PROFILE DEFAULT LIMIT PASSWORD_ROLLOVER_TIME 1/24`, GGADMIN 비밀번호 변경, OGG Credential Store 갱신, `DBLOGIN USERIDALIAS`와 프로세스 재연결 검증, `ALTER USER GGADMIN EXPIRE PASSWORD ROLLOVER PERIOD`로 구 비밀번호 즉시 폐기, 마지막으로 `ALTER PROFILE DEFAULT LIMIT PASSWORD_ROLLOVER_TIME 0` 복원이다. `EXPIRE PASSWORD ROLLOVER PERIOD`는 계정 비밀번호 만료가 아니라 구·신 비밀번호 병행 기간만 즉시 종료하여 새 비밀번호만 허용한다. 이 방식의 위험은 설정 창 동안 DEFAULT 소속 다른 계정도 비밀번호를 변경하면 해당 계정의 구 비밀번호 역시 롤오버 시간 동안 유효해진다는 점이다.
  - 추가 업데이트(15:50): 롤오버 없이 운영 중 DB 비밀번호만 변경하면 기존 인증이 완료된 OGG DB 세션은 즉시 끊기지 않을 수 있지만, Credential Store에 구 비밀번호가 남아 있으므로 이후 프로세스 재시작, DB/네트워크 재연결, failover 등 신규 인증 시 ORA-01017과 ABEND가 발생한다. 따라서 무중단처럼 보이는 것은 기존 세션 수명 동안뿐이며 안전한 운영 절차가 아니다. 롤오버 방식은 DB에서 구·신 비밀번호 병행 기간을 시작한 뒤 Credential Store를 새 비밀번호로 갱신하고 `DBLOGIN USERIDALIAS`와 프로세스 재연결을 검증하여 이 불일치 구간을 제거한다. GGSCI의 `PASSWORD` 절은 값을 평문으로 표시하므로 생략하고 숨김 프롬프트를 사용한다. 대화에 노출된 비밀번호 값은 기록하지 않았으며 운영 비밀번호라면 다시 교체해야 한다.
