# Oracle Incident Log - config


- [2026-08-13 09:14:17] Oracle Incident - config
  - 질문 맥락: net_program/TAC_ConnectionPool.exe.config 전체 설정의 의미, 동작, 수정 포인트 및 주의사항 정리 요청
  - 에러 메시지: 설정 파일에 평문 SYSTEM 계정 정보가 있고 Min Pool Size=20이며 Data Source에는 단일 TCP 주소만 정의되어 있다.
  - 원인 가설: 이 파일은 .NET Framework 4.8.1용 ODP.NET Unmanaged Driver 프로그램의 외부 설정이다. PTSDB 키로 접속 문자열을 로드하고 Pool 20~50개, FAN 기반 HA Events, 풀 반출 시 검증, Application Continuity 요청, 단일 Oracle Net 주소와 patdb 서비스를 사용한다.
  - 권장 조치: SYSTEM 대신 CREATE SESSION과 V_$INSTANCE 조회 권한만 가진 테스트 계정으로 교체하고 비밀번호를 변경한다. 테스트 영향에 맞춰 Min Pool Size를 결정한다. TAC 서비스는 서버 측 FAILOVER_TYPE=AUTO, COMMIT_OUTCOME 및 FAN 설정을 별도로 검증한다. 실제 이중 접속 경로가 필요하면 승인된 SCAN 또는 복수 주소 구성을 사용한다.
  - 검증 항목: 첫 실행 시 최소 풀 세션 수, V$INSTANCE 조회 성공, FAN 후 장애 연결 제거, 다른 서비스 멤버로 재접속, ODP.NET trace 생성 여부를 확인한다. ENABLE=BROKEN은 TCP keepalive이며 TAF/TAC 활성화 설정이 아님을 구분한다.
- [2026-08-13 09:44:56] Oracle Incident - config
  - 질문 맥락: OraOps19.dll 임시 PATH 조치가 아닌 TAC_ConnectionPool.exe 정상 실행을 위한 영구 ODAC 설치 방법 요청
  - 에러 메시지: OraOps19.dll DllNotFoundException 0x8007007E; HKLM Oracle 및 ODP.NET 등록 없음
  - 원인 가설: 배포본은 Oracle.DataAccess.dll 기반 ODP.NET 4 Unmanaged Driver이므로 번들 ODAC19.28 Xcopy의 odp.net4 구성요소와 basic Instant Client 의존성을 설치하고 machine-wide 등록해야 한다.
  - 권장 조치: 관리자 권한 CMD에서 비어 있는 C:\\Oracle\\ODAC1928을 생성하고 ODAC 소스 폴더의 install.bat를 odp.net4 C:\\Oracle\\ODAC1928 ODAC1928 true true 인자로 실행한다. 마지막 true는 machine.config/GAC/ODP.NET 레지스트리 DllPath를 구성하고 앞의 true는 Instant Client 의존성을 설치한다. 인라인 Data Source를 사용하므로 TNS_ADMIN 인자는 생략한다.
  - 검증 항목: install.log 오류 여부, C:\\Oracle\\ODAC1928\\bin\\OraOps19.dll, C:\\Oracle\\ODAC1928\\oci.dll, HKLM\\SOFTWARE\\Oracle\\ODP.NET\\4.122.19.1의 DllPath를 확인한다. 새 콘솔에서 별도 PATH 설정 없이 EXE를 실행해 OraOps19 오류가 사라지는지 확인한다.
- [2026-08-13 09:50:21] Oracle Incident - config
  - 질문 맥락: ODAC 설치 경로 C:\\Oracle\\ODAC1928을 왜 사용하는지 문의
  - 에러 메시지: (없음)
  - 원인 가설: C:\\Oracle\\ODAC1928은 필수 고정값이 아니라 ODAC 19.28 전용 Oracle Home을 식별하기 위해 제안한 짧고 공백 없는 설치 대상 경로다. 배포 원본 폴더와 설치 완료 Oracle Home을 분리하고 한글/공백 경로 관련 배치 및 DLL 탐색 문제를 줄인다.
  - 권장 조치: 쓰기 권한과 운영 표준에 맞는 절대 경로로 변경 가능하되 기존 Oracle Home과 섞지 않고 전용 빈 폴더를 사용한다. 경로를 변경하면 install.bat 인자와 검증 명령에도 동일 경로를 사용한다.
  - 검증 항목: 설치 후 선택한 경로 아래 bin\\OraOps19.dll, oci.dll, install.log가 생성되고 레지스트리 DllPath가 해당 경로의 bin을 가리키는지 확인한다.
- [2026-08-13 09:51:19] Oracle Incident - config
  - 질문 맥락: TAC_ConnectionPool.exe 실행을 위해 Oracle Client를 별도로 다운로드해야 하는지 문의
  - 에러 메시지: 현재 PC에는 Oracle Home 및 ODP.NET 등록이 없지만 배포된 ODAC19.28Xcopy_x64에 instantclient와 odp.net4가 포함되어 있음
  - 원인 가설: Oracle.DataAccess.dll 기반 Unmanaged ODP.NET은 네이티브 Oracle Client가 필요하지만, 제공된 ODAC Xcopy 배포본에 64비트 Instant Client 19.28과 oci.dll이 이미 포함되어 있으므로 별도 다운로드는 필요하지 않다.
  - 권장 조치: install.bat odp.net4 <OracleHomePath> <OracleHomeName> true true로 실행해 odp.net4와 basic Instant Client 의존성을 함께 설치한다. 기존 호환 64비트 Oracle Home을 재사용하는 방식은 현재 PC에는 해당하지 않는다.
  - 검증 항목: 설치 대상에 oci.dll과 bin\\OraOps19.dll이 존재하고 ODP.NET DllPath가 등록된 뒤 EXE의 OraOps19 오류가 사라지는지 확인한다.
- [2026-08-13 10:04:49] Oracle Incident - config
  - 질문 맥락: 설치된 ODAC가 C 드라이브에서 보이지 않는 이유 문의
  - 에러 메시지: (없음)
  - 원인 가설: 설치 대상은 C:\\ODAC1928이 아니라 C:\\Oracle\\ODAC1928이며 실제 폴더와 파일은 존재한다. 사용자가 C 드라이브 루트에서 ODAC1928을 직접 찾고 있거나 탐색기 새로고침 전일 수 있다.
  - 권장 조치: 파일 탐색기 주소창에 C:\\Oracle\\ODAC1928을 직접 입력하거나 C:\\Oracle 폴더 안으로 이동한다.
  - 검증 항목: 탐색기 또는 Get-Item으로 C:\\Oracle\\ODAC1928 및 oci.dll, bin 폴더 존재를 확인한다.
- [2026-08-13 10:29:19] Oracle Incident - config
  - 질문 맥락: SCAN IP 접속이면 Pool 세션이 RAC 인스턴스 양쪽으로 분할되어야 하는지 문의
  - 에러 메시지: (없음)
  - 원인 가설: SCAN은 연결 요청을 서비스 제공 인스턴스 중 적절한 대상으로 리디렉션하지만 정확한 50:50을 즉시 보장하지 않는다. 현재 DMHOME은 양 인스턴스 READY, CLB_GOAL=LONG이며 SCAN Listener도 두 인스턴스를 등록하고 있다.
  - 권장 조치: 인스턴스별 Pool 분포를 GV$SESSION으로 시간대별 확인한다. TAC 검증에는 기본 DMHOME 서비스가 아니라 FAILOVER_TYPE=AUTO, COMMIT_OUTCOME=YES, AQ_HA_NOTIFICATIONS=YES인 별도 애플리케이션 서비스를 구성해야 한다.
  - 검증 항목: 초기 스냅샷에서는 DMHOME2에 20개가 보였으나 최신 스냅샷에서는 동일 실행 중 Pool 총 20개가 DMHOME1 10개, DMHOME2 10개로 분산됐다. SCAN Listener 192.168.40.195:11521은 DMHOME1과 DMHOME2를 모두 READY로 보고 있다.
- [2026-08-13 12:19:22] Oracle Incident - config
  - 질문 맥락: TAC_EXPLICIT_TX.exe가 실제 TAC 테스트 프로그램인지와 올바른 TAC 구성 조건 확인
  - 에러 메시지: 인스턴스 shutdown 시 ORA-03113: end-of-file on communication channel 발생 후 프로그램 종료
  - 원인 가설: 실행 파일은 TAC 시험을 의도했으나 Oracle 서비스 DMHOME에 TAC가 설정되지 않았고, 프로그램도 명시적 트랜잭션 및 Commit 없이 예외를 출력하고 종료하여 Transaction Guard와 replay를 검증할 수 없음
  - 권장 조치: DMHOME_TAC 전용 Clusterware 서비스를 failovertype AUTO 등으로 구성하고 exe.config를 해당 서비스로 변경한 뒤, BeginTransaction과 Commit을 포함하는 요청 단위 테스트 프로그램으로 재빌드
  - 검증 항목: 현재 DMHOME 서비스의 FAILOVER_TYPE NONE, COMMIT_OUTCOME NO 확인. 실행 파일 IL에서 BeginTransaction 및 Commit 호출 없음. 연결 문자열 SERVICE_NAME=DMHOME 사용 확인.
- [2026-08-13 15:44:03] Oracle Incident - config
  - 질문 맥락: RAC TAC 서비스를 반드시 srvctl로 만들어야 하는지, tnsnames.ora 등록만으로 가능한지
  - 에러 메시지: (not provided)
  - 원인 가설: tnsnames.ora는 클라이언트 connect descriptor이며 SERVICE_NAME과 주소, TAF FAILOVER_MODE 등을 지정할 뿐 서버 서비스의 TAC 속성 FAILOVER_TYPE=AUTO, COMMIT_OUTCOME=TRUE, FAILOVER_RESTORE=AUTO, NOTIFICATION=TRUE를 생성하거나 변경하지 못함. Oracle Clusterware가 관리하는 RAC 서비스는 srvctl로 구성해야 함.
  - 권장 조치: srvctl로 DMHOME_TAC 전용 RAC 서비스를 생성 및 시작한 다음 클라이언트 config 또는 tnsnames alias의 SERVICE_NAME을 DMHOME_TAC으로 변경한다. DBMS_SERVICE는 Clusterware 관리 RAC 서비스 대신 사용하지 않는다.
  - 검증 항목: srvctl config service -db DMHOME -service DMHOME_TAC과 DBA_SERVICES에서 AUTO, TRUE 속성을 확인하고 양 인스턴스 서비스 상태를 확인한 뒤 재시험한다.
- [2026-08-13 17:13:41] Oracle Incident - config
  - 질문 맥락: RAC 두 인스턴스가 실행 중인데 TAC_ConnectionPool.exe의 Min Pool Size 20개가 모두 DMHOME1에 생성되는 이유
  - 에러 메시지: 오류 없음. GV$SESSION 집계에서 DMHOME1 SYSTEM DMHOME_TAC TAC_ConnectionPool.exe INACTIVE 20건, DMHOME2 0건
  - 원인 가설: DMHOME_TAC 서비스가 Cardinality 1, Preferred DMHOME1, Available DMHOME2인 singleton active-passive 배치이므로 평상시 서비스가 DMHOME1에만 등록되고 SCAN이 모든 연결을 그 서비스 제공 인스턴스로 전달함
  - 권장 조치: 현재 구성은 TAC 장애복구 시험에는 정상이다. 평상시 양 노드 분산이 필요하면 DMHOME2도 preferred로 전환하여 서비스를 양 인스턴스에서 실행하고 RLB goal 및 ODP.NET Load Balancing 설정을 별도로 구성한다.
  - 검증 항목: srvctl status/config service에서 Service on DMHOME1, Cardinality 1, Preferred DMHOME1, Available DMHOME2, CLB LONG, RLB NONE 확인. 양 인스턴스 RUNNING 확인.
- [2026-08-13 17:14:51] Oracle Incident - config
  - 질문 맥락: DMHOME_TAC을 Active-Active로 구성해야 정상 인스턴스 연결은 유지하고 장애 인스턴스 연결만 생존 인스턴스로 이동하는지 확인
  - 에러 메시지: 오류 없음. 현재 서비스는 Preferred DMHOME1, Available DMHOME2, Cardinality 1인 Active-Standby 구성
  - 원인 가설: 사용자가 원하는 정상 시 양 노드 분산과 장애 시 부분 이동은 DMHOME1과 DMHOME2를 모두 Preferred로 두는 Active-Active 서비스 구성에서 구현됨
  - 권장 조치: DMHOME2를 Available에서 Preferred로 전환해 서비스 Cardinality 2로 구성하고, RLB SERVICE_TIME 및 ODP.NET Load Balancing=true를 사용한다. 기존 풀은 재시작하여 새 배치로 다시 생성한다.
  - 검증 항목: 서비스가 양 인스턴스에서 RUNNING인지 확인하고 GV$SESSION을 인스턴스별 집계한다. 한 인스턴스 장애 시 정상 노드 세션은 유지되고 장애 노드 요청은 TAC replay되어 생존 노드로 이동하는지 확인한다.
