# Oracle Incident Log - config


- [2026-08-21 12:25:49] Oracle Incident - config
  - 질문 맥락: 추가 업데이트: LOG_ARCHIVE_CONFIG DG_CONFIG 사용 시 FAL_SERVER가 불필요한지와 Far Sync topology 권장값
  - 에러 메시지: 현재 ACTIVE FAL_SERVER=FSYNC 단일값이어서 FSYNC 경로가 ORA-16086/16055로 막혔을 때 Primary에서 gap을 직접 가져올 fallback이 없음.
  - 원인 가설: DG_CONFIG는 Data Guard 구성원 DB_UNIQUE_NAME 목록과 전송 허용/검증을 담당하고 FAL 서버 접속 순서를 제공하지 않는다. Oracle 19c Far Sync 예제는 Far Sync FAL_SERVER=Primary, terminal standby FAL_SERVER=FarSync,Primary로 구성한다.
  - 권장 조치: DMHOME/FSYNC/ACTIVE 모두 DG_CONFIG를 동일하게 유지. FSYNC는 FAL_SERVER=DMHOME_PROD 유지. ACTIVE는 FAL_SERVER=FSYNC,DMHOME_PROD로 구성하여 Far Sync 우선, Primary fallback gap fetch를 제공한다. Primary는 현재 역할에서는 FAL을 사용하지 않지만 role transition 대비 ACTIVE를 둘 수 있다.
  - 검증 항목: ACTIVE에서 gap 발생 시 FSYNC 실패 후 DMHOME_PROD로 FAL 요청이 이어지고 T1S87이 수신·등록되어 MRP WAIT_FOR_GAP이 해제되는지 확인.
- [2026-08-21 12:27:59] Oracle Incident - config
  - 질문 맥락: 추가 업데이트: Far Sync 구성 초안 검토, VALID_FOR 생략 가능 여부 및 ADG 현재 상태
  - 에러 메시지: ACTIVE는 READ ONLY WITH APPLY, MRP0 WAIT_FOR_LOG T1S97. FSYNC에는 T1S97 archive가 존재하지만 DEST_2 ORA-03135; ACTIVE FAL_SERVER=FSYNC 단일값. RFS trace는 source nphpagedb2 연결에서 ORA-03113 수신 종료.
  - 원인 가설: VALID_FOR는 생략 시 ALL_LOGFILES,ALL_ROLES이므로 문법상 필수가 아니며 현재 ORA-03135의 직접 원인으로 확정할 수 없다. 다만 역할 전환 및 Far Sync 경로 명확화를 위해 권장된다. 현재 지속 gap의 구성상 문제는 ACTIVE에 Primary fallback FAL server가 없어 FSYNC 전송 장애 시 직접 gap fetch가 불가능한 점이다.
  - 권장 조치: ACTIVE FAL_SERVER를 FSYNC,DMHOME_PROD 순서로 구성. 문서에는 FSYNC LOG_ARCHIVE_DEST_2와 ACTIVE FAL_SERVER/LOG_ARCHIVE_DEST_1을 추가하고, 안정 상태의 protection_level은 MAXIMUM AVAILABILITY임을 수정한다. VALID_FOR는 권장값으로 표시하되 필수라고 단정하지 않는다.
  - 검증 항목: ACTIVE가 T1S97을 FSYNC 또는 DMHOME_PROD에서 가져와 적용하고 MRP가 이후 최신 sequence를 따라감. FSYNC DEST_2가 VALID로 복귀하고 RFS/TT 연결이 log switch 후에도 유지되는지 확인.
- [2026-08-21 12:59:33] Oracle Incident - config
  - 질문 맥락: 사용자가 작성한 Oracle 19c Far Sync 이론 및 구성 문서에서 틀린 내용 검토
  - 에러 메시지: 운영 문서에 ACTIVE TNS 포트 검증 누락, Far Sync 지원 모드에 MAXIMUM PROTECTION 포함, 안정 목표를 RESYNCHRONIZATION으로 표기, 실제 DB_UNIQUE_NAME과 예시 이름 혼용
  - 원인 가설: 개념 설명과 실제 환경값이 혼합되고 일시적 장애 상태를 정상 목표로 오인했으며 Oracle Net service와 DB_UNIQUE_NAME을 구분하지 않음
  - 권장 조치: DG_CONFIG에는 DMHOME,FSYNC,ACTIVE 실제 DB_UNIQUE_NAME 사용. TNS ACTIVE는 191:11521 검증. Primary/Far Sync/Standby 각 DEST와 STATE, VALID_FOR 권장값, 로컬 archive destination을 명시. Terminal FAL_SERVER는 FSYNC와 DMHOME_PROD를 별도 ordinal로 설정. Far Sync 지원 모드는 Maximum Availability/Performance로 한정하고 안정 PROTECTION_LEVEL은 Maximum Availability로 정정.
  - 검증 항목: V$ARCHIVE_DEST, V$ARCHIVE_DEST_STATUS, V$MANAGED_STANDBY, V$DATAGUARD_STATS 및 lsnrctl services로 문서값과 실제 상태 대조
